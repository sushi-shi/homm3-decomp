interface:
  display_name: "Holista"
  short_description: "Recover natural source and inlined helper boundaries"
  default_prompt: "Use $holista to pair the primary matcher with Holista, reconstruct this difficult function as its original developers plausibly wrote it, and recognize inlined and nested helpers from temporary lifetimes, challenge artificial caller scopes, and test the complete source model against retail."
