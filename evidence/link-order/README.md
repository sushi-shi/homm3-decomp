# Retail link-order analysis

GENERATED by `python -m homm3.analysis.link_order`; regenerate, never hand-edit.

Own-source origin annotations bracket each unit's retail span. Header-origin functions are residents, not anchors. An origin expires at an intervening unclaimed declaration or body; it cannot attach to a later unrelated claim.

The current source supplies 115 spanned units, 2,215 own-source anchors and 27 header-origin residents. Alphabetical order: True. Span overlaps: 0.

Within the bounded interval `0x2b10..0x202cb3`, 4,659 carved functions have a relation: 3,138 lie inside spans and 1,521 lie between them. There are 933 unclaimed in-span functions.

See `units.tsv`, `order.tsv`, `gaps.tsv`, `attribution.tsv` and `functions.tsv` for generated details. These are ownership leads, not byte-match verdicts or proof of whole-TU closure. Units without explicit source-origin annotations are not automatically spanned; no origin is invented for them. Functions outside the bounded interval require separate review.
