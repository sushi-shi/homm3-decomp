// multiplayerwindow.h - prototypes of multiplayerwindow.cpp (compiland multiplayerwindow.obj)
// HAND-OWNED after admission - INITIALLY GENERATED by `python3 -m homm3.carve carcass`.
// Class layouts are NOT fabricated from method symbols;
// prototypes stay comments until a retail layout is proven.
#ifndef HOMM3_MULTIPLAYERWINDOW_H
#define HOMM3_MULTIPLAYERWINDOW_H

#include <windows.h>
#include <string.h>
#include "window.h"
#include "textwdgt.h"
#include "textntry.h"
#include "netgame.h"
#include "va.h"
// CDPlaySession and CAutoArray<T> live with their owning compiland; this
// header used to carry private copies of both behind a per-TU macro.
#include "dxplay.h"

struct _DPCOMPORTADDRESS;

// multiplayerwindow.cpp owns this 21-byte preference-backed player name.
// DoNewGame and the campaign launch copy it into a player cName slot.
extern char g_localPlayerName[21];
// Before normalization: gUnnamed69927c.
extern int g_unnamed69927c;
// Before normalization: gUnnamed699288.
extern int g_unnamed699288;

// Cast-free storage for the Winsock bind call in GetIPAddress. Both views are
// the same 16-byte IPv4 socket-address record; keeping the union in the domain
// header avoids a TU-local layout view.
union TIPv4SocketAddress {
    // Before normalization: internet.
    sockaddr_in m_internet;
    // Before normalization: generic.
    sockaddr m_generic;
};
SIZE(TIPv4SocketAddress, 0x10);

#include "hotseat.h"

// The text-entry widgets the two multiplayer dialogs use. All three add the
// same doubly-linked next/prev pair (@0x70/@0x74) to textEntryWidget so a
// dialog can chase focus around its field ring; the hierarchy is proven by
// the CMPInputDlg/CHotSeatDlg constructors and the four vtables 0x640184
// (CMPEdit), 0x640130 (CMPInputEdit), 0x640210 (CHotSeatEdit).
//
// CMPEdit's constructor is emitted OUT OF LINE at retail 0x510760 (it stores
// vtable 0x640184 and zeros the two links); CMPInputEdit derives it and its
// own constructor is inline, so `new CMPInputEdit` calls 0x510760 then stores
// 0x640130. CHotSeatEdit derives textEntryWidget directly with an inline
// constructor: this is the only model that preserves the retail constructor's
// later vector-inlining state. Its DC OnKeyPress nevertheless reuses
// CMPEdit::OnKeyPress, and retail table 0x640210 folds the identical
// SetFocus/OnNextEdit/OnPrevEdit bodies onto CMPEdit's addresses.
// CMPEdit overrides SetFocus(14)/OnKeyPress(15) and introduces the virtual
// OnPrevEdit(19)/OnNextEdit(20) pair; CMPInputEdit re-overrides
// OnKeyPress(15); CHotSeatEdit overrides OnKillFocus(11)/SetFocus(14)/
// OnKeyPress(15) and carries its own ring-walk pair.
class CMPEdit : public textEntryWidget {
public:
    // Before normalization: nextEdit.
    CMPEdit* m_nextEdit;   // +0x70
    // Before normalization: prevEdit.
    CMPEdit* m_prevEdit;   // +0x74

    CMPEdit(int x, int y, int w, int h, int textSize, const char* text,
            const char* fontName, font::TColor color, unsigned justification,
            const char* backgroundIcon, int backgroundFrame, int id,
            int style, int readType, int insetX, int insetY);
    // Before normalization (function): CMPEdit::SetNextEdit.
    // Before normalization (locals): pNextEdit.
    void setNextEdit(CMPEdit* nextEdit) { m_nextEdit = nextEdit; }
    // Before normalization (function): CMPEdit::SetPrevEdit.
    // Before normalization (locals): pPrevEdit.
    void setPrevEdit(CMPEdit* prevEdit) { m_prevEdit = prevEdit; }
    // Before normalization (function): CMPEdit::SetFocus.
    virtual void setFocus(unsigned char state);  // slot 14, retail 0x510890
    // Before normalization (function): CMPEdit::OnKeyPress.
    virtual int onKeyPress(message* msg);        // slot 15, retail 0x5107d0
    // DECLARATION ORDER CORRECTED 2026-09-06 (claim lane 31): retail's own
    // table 0x640184 holds 0x510850 at slot 19 and 0x510870 at slot 20, and
    // the two bodies are told apart by the member they read - 0x510850 reads
    // nextEdit at +0x70, 0x510870 reads prevEdit at +0x74. DC lists
    // OnNextEdit (dc 0x10215c) ahead of OnPrevEdit (dc 0x102184) for the same
    // reason. The pair had been declared the other way round, which put the
    // WRONG body in each vtable slot; the call sites below move with it, so
    // no emitted instruction changes.
    // Before normalization (function): CMPEdit::OnNextEdit.
    virtual void onNextEdit();                   // slot 19, retail 0x510850
    // Before normalization (function): CMPEdit::OnPrevEdit.
    virtual void onPrevEdit();                   // slot 20, retail 0x510870
};

class CMPInputEdit : public CMPEdit {
public:
    CMPInputEdit(int x, int y, int w, int h, int textSize, const char* text,
                 const char* fontName, font::TColor color,
                 unsigned justification, const char* backgroundIcon,
                 int backgroundFrame, int id, int style, int readType,
                 int insetX, int insetY)
        : CMPEdit(x, y, w, h, textSize, text, fontName, color, justification,
                  backgroundIcon, backgroundFrame, id, style, readType, insetX,
                  insetY)
    {
    }
    // Before normalization (function): CMPInputEdit::OnKeyPress.
    virtual int onKeyPress(message* msg);         // slot 15, retail 0x50de50
};

class CHotSeatEdit : public textEntryWidget {
public:
    // Before normalization: nextEdit.
    CHotSeatEdit* m_nextEdit;   // +0x70
    // Before normalization: prevEdit.
    CHotSeatEdit* m_prevEdit;   // +0x74

    CHotSeatEdit(int x, int y, int w, int h, int textSize, const char* text,
                 const char* fontName, font::TColor color,
                 unsigned justification, const char* backgroundIcon,
                 int backgroundFrame, int id, int style, int readType,
                 int insetX, int insetY)
        : textEntryWidget(x, y, w, h, textSize, text, fontName, color,
                          justification, backgroundIcon, backgroundFrame, id,
                          style, readType, insetX, insetY)
    {
        m_nextEdit = 0;
        m_prevEdit = 0;
    }
    // Before normalization (function): CHotSeatEdit::SetNextEdit.
    // Before normalization (locals): pNextEdit.
    void setNextEdit(CHotSeatEdit* nextEdit) { m_nextEdit = nextEdit; }
    // Before normalization (function): CHotSeatEdit::SetPrevEdit.
    // Before normalization (locals): pPrevEdit.
    void setPrevEdit(CHotSeatEdit* prevEdit) { m_prevEdit = prevEdit; }
    // Before normalization (function): CHotSeatEdit::OnKillFocus.
    virtual void onKillFocus();                   // slot 11, retail 0x50dee0
    // Before normalization (function): CHotSeatEdit::SetFocus.
    virtual void setFocus(unsigned char state);   // slot 14, retail 0x510890
    // Before normalization (function): CHotSeatEdit::OnKeyPress.
    virtual int onKeyPress(message* msg);         // slot 15, retail 0x50df60
    // Before normalization (function): CHotSeatEdit::OnNextEdit.
    virtual void onNextEdit();                    // slot 19, folded 0x510850
    // Before normalization (function): CHotSeatEdit::OnPrevEdit.
    virtual void onPrevEdit();                    // slot 20, folded 0x510870
};

// DC derives CHotSeatDlg from CHeroWindowEx and places its `edit` run at
// +0x4c, followed by m_rollover at +0x6c. Retail's independently proven
// CHeroWindowEx is four bytes wider, and the retail getter at 0x512530 reads
// m_rollover at +0x70, proving the corresponding repack. DC identifies the
// edit run as textWidget*[8]; retail OnOK corroborates its +0x50 start, four-
// byte stride, and textWidget::Text access.
class CHotSeatDlg : public CHeroWindowEx {
public:
    enum {
        BACKGROUND_ID = 500,
        FIRST_EDIT_ID = 509,
        HEADER_ID = 517,
        ROLLOVER_ID = 518,
        OKAY_ID = 519,
        BACK_ID = 520
    };

    // Before normalization: edit.
    CHotSeatEdit* m_edit[8];       // +0x50
    textWidget* m_rollover;      // +0x70
    // Before normalization: gHotSeatHelp.
    THelpText m_hotSeatHelp[20];  // +0x74, DC identity/extent after repack

    CHotSeatDlg();
    virtual ~CHotSeatDlg();
    // Before normalization (function): CHotSeatDlg::OnWidgetDeselect.
    virtual int onWidgetDeselect(int id, bool& exitFlag);
    // Before normalization (function): CHotSeatDlg::GetRolloverWidget.
    virtual textWidget* getRolloverWidget();
    // Before normalization (function): CHotSeatDlg::OnOK.
    unsigned char onOK();
    // Non-virtual, and the vtable proves it: 0x6401d8 stops after slot 13
    // (0x240210, CHotSeatEdit's table, starts at +0x38). Retail emits no
    // body for these helpers - they expand into CHotSeatEdit's two overrides.
    // DC separates UpdateOK (dc 0x102d4c), which only updates widget 519,
    // from OnKillFocus (dc 0x102cc8), which then redraws the dialog.
    // Before normalization (function): CHotSeatDlg::GetPlayerCount.
    int getPlayerCount();
    // Before normalization (function): CHotSeatDlg::UpdateOK.
    void updateOK();
    // Before normalization (function): CHotSeatDlg::OnKillFocus.
    void onKillFocus(int id);
};
SIZE(CHotSeatDlg, 0x114);

// CMPInputDlg - a CHeroWindowEx text-entry dialog (host name / password).
// DC field list 0x4493 (base CHeroWindowEx @0, DC size 0x60) lays out
// field1@0x4c, field2@0x50 (CMPInputEdit*), header1@0x54, header2@0x58,
// rollover@0x5c (textWidget*). Retail's CHeroWindowEx is four bytes wider,
// so every member shifts +4: the getter at 0x510970 reads rollover@0x60 and
// OnWidgetDeselect reads field1@0x50 (status@0x16 & WIDGET_ACTIVE, Text@0x30).
// The vtable 0x6400f4 is FIFTEEN slots, not fourteen: it runs 0x2400f4 to
// 0x24012f and CMPInputEdit's own table starts at 0x240130, so slot 14 is
// real and holds 0x510980 - UpdateOK. That is the one place this dialog
// diverges from CHotSeatDlg's roster (whose table stops at slot 13), and
// CMPInputEdit::OnKeyPress 0x50de50 calls it through `[edx+0x38]` rather
// than inlining it, which is the other half of the same proof.
// DisableOK/OnOK stay non-virtual. field1/field2 are DC CMPInputEdit* but
// reached only as textWidget here.
class CMPInputDlg : public CHeroWindowEx {
public:
    enum {
        BACKGROUND_ID = 500,
        FIELD1_ID = 501,
        FIELD2_ID = 502,
        HEADER1_ID = 503,
        HEADER2_ID = 504,
        OKAY_ID = 505,
        BACK_ID = 506,
        ROLLOVER_ID = 507
    };

    // Original members: CMPInputDlg::field1/field2 (DC class 0x4484,
    // +0x4c/+0x50). These name the two text-entry fields, not unknown
    // offsets. The PC base shifts both by four bytes; retain OG names.
    CMPInputEdit* m_field1;  // +0x50
    CMPInputEdit* m_field2;  // +0x54
    // Before normalization: header1.
    textWidget* m_header1;   // +0x58
    // Before normalization: header2.
    textWidget* m_header2;   // +0x5c
    // Before normalization: rollover.
    textWidget* m_rollover;  // +0x60

    __forceinline CMPInputDlg(int maxChars1, int maxChars2);
    virtual ~CMPInputDlg();
    // Before normalization (function): CMPInputDlg::OnWidgetDeselect.
    // Before normalization (locals): bExitFlag.
    virtual int onWidgetDeselect(int id, bool& exitFlag);
    // Before normalization (function): CMPInputDlg::GetRolloverWidget.
    virtual textWidget* getRolloverWidget();
    // Before normalization (function): CMPInputDlg::OnOK.
    unsigned char onOK();
    // Before normalization (function): CMPInputDlg::UpdateOK.
    virtual void updateOK();  // slot 14, retail 0x510980
    // Before normalization (function): CMPInputDlg::DisableOK.
    __forceinline void disableOK()
    {
        getWidget(OKAY_ID)->enable(0);
    }
};
SIZE(CMPInputDlg, 0x64);

class CSprite;

class CHeroSessions : public CAutoArray<CDPlaySession> {
public:
    enum eSessionStatus {
        closed,
        open,
        password
    };

    // Before normalization (function): CHeroSessions::GetSessionInfo.
    bool getSessionInfo(unsigned long index, char* sessName, char* userName,
                        int& numPlayers, eSessionStatus& status);
    // Before normalization (function): CHeroSessions::GetSessionInfo.
    bool getSessionInfo(int index, char* sessName, char* userName,
                        int& numPlayers, eSessionStatus& status);
};
SIZE(CHeroSessions, 0x14);

// CMultiPlayerWindowEdit - the text-entry widget the session-host name field
// uses. Derives textEntryWidget, forwarding all sixteen constructor arguments;
// its only addition is the slot-15 key-handler override that gives it a
// distinct vtable (retail 0x640054, stored by the TMultiPlayerWindow
// constructor).
class CMultiPlayerWindowEdit : public textEntryWidget {
public:
    CMultiPlayerWindowEdit(int x, int y, int w, int h, int textSize,
                           const char* text, const char* fontName,
                           font::TColor color, unsigned justification,
                           const char* backgroundIcon, int backgroundFrame,
                           int id, int style, int readType, int insetX,
                           int insetY)
        : textEntryWidget(x, y, w, h, textSize, text, fontName, color,
                          justification, backgroundIcon, backgroundFrame, id,
                          style, readType, insetX, insetY)
    {
    }
    // Before normalization (function): CMultiPlayerWindowEdit::OnKeyPress.
    virtual int onKeyPress(message* msg);  // slot 15, retail 0x50ed60
};

// TMultiPlayerWindow - CHeroWindowEx multiplayer session browser / host UI.
// DC field list 0x472e (base CHeroWindowEx @0, DC size 252); retail's four-
// byte-wider base shifts every trailing member +4 (all are pointers, a char
// array, or small scalars, so the shift is uniform). Retail proofs:
// GetRolloverWidget reads RolloverWidget@0xfc; InitRemote reads playerName
// @0xbc and sessTimer@0x68; ~dtor reads GameState@0x50 and pSessions@0x60;
// GoSessionList/GoMainMenu toggle the button run @0xc4..0xf8. Total 0x100.
// The 14-slot vtable 0x6400a0 overrides slot 0 (sdd/dtor), 9 (WindowHandler),
// 12 (OnWidgetDeselect), 13 (GetRolloverWidget).
//
// The DC types of the widget members are bitmapBorder* (splash), button*
// (the ten screen buttons), slider* (gameSlider) and textWidget* (the three
// headers); they are modelled as their widget/textWidget base here because
// every reconstructed body reaches them only through widget::send_message /
// textWidget::Text, and narrowing the include set avoids the declarator wall.
class TMultiPlayerWindow : public CHeroWindowEx {
public:
    enum EWidgetId {
        ONLINE_ID = 101,
        HOT_SEAT_ID = 102,
        IPX_ID = 103,
        TCP_ID = 104,
        MODEM_ID = 105,
        DIRECT_ID = 106,
        HOST_ID = 107,
        JOIN_ID = 108,
        SEARCH_ID = 109,
        FIRST_SESSION_ID = 110,
        LAST_SESSION_ID = 121,
        GAME_SLIDER_ID = 122,
        ROLLOVER_ID = 123,
        CANCEL_ID = 124,
        PLAYER_NAME_ID = 125,
        IP_ADDRESS_ID = 126
    };

    // Before normalization: GameState.
    CSprite* m_gameState;                     // +0x50
    // Before normalization: inSessionList.
    unsigned char m_inSessionList;            // +0x54
    // Before normalization: showSplash.
    unsigned char m_showSplash;               // +0x55
    // Before normalization: currentGame.
    int m_currentGame;                        // +0x58
    // Before normalization: currentIndex.
    int m_currentIndex;                       // +0x5c
    // Before normalization: pSessions.
    CHeroSessions* m_sessions;               // +0x60
    // Before normalization: sessTimer.
    unsigned long m_sessTimer;                // +0x64
    // Before normalization: sessionRefreshTimeout.
    unsigned long m_sessionRefreshTimeout;    // +0x68
    // Before normalization: localIPAddress.
    char m_localIpAddress[80];                // +0x6c
    // Before normalization: playerName.
    textWidget* m_playerName;                 // +0xbc (DC textEntryWidget*)
    // Before normalization: hostJoinScreen.
    unsigned char m_hostJoinScreen;           // +0xc0
    // Before normalization: splash.
    widget* m_splash;                         // +0xc4 (DC bitmapBorder*)
    // Before normalization: hotSeat.
    widget* m_hotSeat;                        // +0xc8 (DC button*)
    // Before normalization: ipx.
    widget* m_ipx;                            // +0xcc
    // Before normalization: tcp.
    widget* m_tcp;                            // +0xd0
    // Before normalization: modem.
    widget* m_modem;                          // +0xd4
    // Before normalization: direct.
    widget* m_direct;                         // +0xd8
    // Before normalization: online.
    widget* m_online;                         // +0xdc
    // Before normalization: host.
    widget* m_host;                           // +0xe0
    // Before normalization: join.
    widget* m_join;                           // +0xe4
    // Before normalization: search.
    widget* m_search;                         // +0xe8
    // Before normalization: cancel.
    widget* m_cancel;                         // +0xec
    // Before normalization: gameSlider.
    widget* m_gameSlider;                     // +0xf0 (DC slider*)
    // Before normalization: sessNameHeader.
    textWidget* m_sessNameHeader;             // +0xf4
    // Before normalization: userNameHeader.
    textWidget* m_userNameHeader;             // +0xf8
    // Before normalization: RolloverWidget.
    textWidget* m_rolloverWidget;             // +0xfc

    TMultiPlayerWindow();
    virtual ~TMultiPlayerWindow();
    // Before normalization (function): TMultiPlayerWindow::WindowHandler.
    virtual int windowHandler(message* msg);
    // Before normalization (function): TMultiPlayerWindow::OnWidgetDeselect.
    // Before normalization (locals): bExitFlag.
    virtual int onWidgetDeselect(int id, bool& exitFlag);
    // Before normalization (function): TMultiPlayerWindow::GetRolloverWidget.
    virtual textWidget* getRolloverWidget();
    // Before normalization (function): TMultiPlayerWindow::GoSessionList.
    void goSessionList();
    // Before normalization (function): TMultiPlayerWindow::GoMainMenu.
    void goMainMenu();
    // Before normalization (function): TMultiPlayerWindow::Update.
    void update();
    // Before normalization (function): TMultiPlayerWindow::JoinSession.
    // Before normalization (locals): pSession.
    unsigned char joinSession(CDPlaySession* session, const char* password);
    // Before normalization (function): TMultiPlayerWindow::OnHost.
    unsigned char onHost();
    // Before normalization (function): TMultiPlayerWindow::OnJoin.
    unsigned char onJoin();
    // Before normalization (function): TMultiPlayerWindow::OnTCP.
    unsigned char onTCP();
    // Before normalization (function): TMultiPlayerWindow::OnIPX.
    unsigned char onIPX();
    // Before normalization (function): TMultiPlayerWindow::OnSearch.
    unsigned char onSearch();
    // Before normalization (function): TMultiPlayerWindow::OnHotSeat.
    unsigned char onHotSeat();
    // Before normalization (function): TMultiPlayerWindow::OnModem.
    unsigned char onModem();
    // Before normalization (function): TMultiPlayerWindow::OnDirect.
    unsigned char onDirect();
    // Before normalization (function): TMultiPlayerWindow::HostSession.
    unsigned char hostSession(const char* sessName, const char* password);
    // Before normalization (function): TMultiPlayerWindow::InitRemote.
    // Before normalization (locals): sExtra.
    unsigned char initRemote(eNetGameType netGameType, const char* extra,
                             _DPCOMPORTADDRESS* comportInfo);
    // Before normalization (function): TMultiPlayerWindow::OnModemHost.
    unsigned char onModemHost();
    // Before normalization (function): TMultiPlayerWindow::OnDirectHost.
    unsigned char onDirectHost();
    // Before normalization (function): TMultiPlayerWindow::OnModemJoin.
    unsigned char onModemJoin();
    // Before normalization (function): TMultiPlayerWindow::OnDirectJoin.
    unsigned char onDirectJoin();
};
SIZE(TMultiPlayerWindow, 0x100);

// The singleton the ctor latches to `this` (0x50e050+0x66) and the dtor
// nulls (0x50ee40+0x51). No DC public names it - provisional house name.
// Before normalization: gpMultiPlayerWindow.
DATA(0x0069ca28) extern TMultiPlayerWindow* g_multiPlayerWindow;

// --- globals ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:94, dc 0xffaac) void AddHelp(THelpText* pHelpText, const char* rollover, const char* RightClick);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:872, dc 0xffb40) void DeleteTempSaveGame(const char* filename);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1005, dc 0x100408) void SliderGames(int state, heroWindow* parent_window);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1822, dc 0x101780) unsigned char GetIPAddress(char* sIPAddress);

// --- CAutoArray<CDPlaySession> ---
// CODEVIEW(E:\gamedcs\array.h:37, dc 0x103054) void CAutoArray<CDPlaySession>::CAutoArray<CDPlaySession>();
// CODEVIEW(E:\gamedcs\array.h:46, dc 0x103084) void CAutoArray<CDPlaySession>::~CAutoArray<CDPlaySession>();
// CODEVIEW(E:\gamedcs\array.h:73, dc 0x1030bc) unsigned char CAutoArray<CDPlaySession>::Add(CDPlaySession* element);
// CODEVIEW(E:\gamedcs\array.h:95, dc 0x103124) CDPlaySession* CAutoArray<CDPlaySession>::Get(unsigned long elementNbr);
// CODEVIEW(E:\gamedcs\array.h:103, dc 0x103138) unsigned char CAutoArray<CDPlaySession>::Put(unsigned long elementNbr, CDPlaySession* element);
// CODEVIEW(E:\gamedcs\array.h:113, dc 0x103150) unsigned char CAutoArray<CDPlaySession>::Delete(unsigned long elementNbr);
// CODEVIEW(E:\gamedcs\array.h:127, dc 0x10318c) unsigned char CAutoArray<CDPlaySession>::Insert(unsigned long nextElementNbr, CDPlaySession* element);
// CODEVIEW(E:\gamedcs\array.h:144, dc 0x1031fc) unsigned long CAutoArray<CDPlaySession>::GetCount();
// CODEVIEW(..\stlport\stl_string.h:144, dc 0x103200) void* CAutoArray<CDPlaySession>::`scalar deleting destructor'(unsigned __flags);

// --- CDPlay ---
// CODEVIEW(E:\gamedcs\dxplay.h:375, dc 0x101d98) long CDPlay::GetLastError();

// --- CDPlaySession ---
// CODEVIEW(E:\gamedcs\dxplay.h:82, dc 0x101d58) unsigned char CDPlaySession::IsJoinDisabled();
// CODEVIEW(E:\gamedcs\dxplay.h:96, dc 0x101d84) unsigned char CDPlaySession::IsPasswordProtected();

// --- CHeroSessions ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:174, dc 0x101f34) unsigned char CHeroSessions::GetSessionInfo(unsigned long index, char* sessName, char* userName, int* numPlayers, CHeroSessions::eSessionStatus* status);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1000, dc 0x102fcc) void CHeroSessions::CHeroSessions();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1000, dc 0x103008) void* CHeroSessions::`scalar deleting destructor'(unsigned __flags);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1000, dc 0x10303c) void CHeroSessions::~CHeroSessions();

// --- CHotSeatDlg ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:629, dc 0x1028c4) void CHotSeatDlg::CHotSeatDlg();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:700, dc 0x102c28) void CHotSeatDlg::~CHotSeatDlg();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:707, dc 0x102c6c) int CHotSeatDlg::OnWidgetDeselect(int id, unsigned char* bExitFlag);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:729, dc 0x102cc8) void CHotSeatDlg::OnKillFocus(int id);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:737, dc 0x102cf8) int CHotSeatDlg::GetPlayerCount();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:750, dc 0x102d4c) void CHotSeatDlg::UpdateOK();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:756, dc 0x102d88) unsigned char CHotSeatDlg::OnOK();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:780, dc 0x102e1c) textWidget* CHotSeatDlg::GetRolloverWidget();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:786, dc 0x102e24) int CHotSeatDlg::WindowHandler(message* msg);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:844, dc 0x102f64) void* CHotSeatDlg::`scalar deleting destructor'(unsigned __flags);

// --- CHotSeatEdit ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:854, dc 0xffaec) void CHotSeatEdit::OnKillFocus();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:860, dc 0xffb0c) int CHotSeatEdit::OnKeyPress(message* msg);

// --- CHotSeatMan ---
// CODEVIEW(E:\gamedcs\remote.h:186, dc 0x101da8) void CHotSeatMan::CHotSeatMan();
// CODEVIEW(E:\gamedcs\remote.h:192, dc 0x101dc4) void CHotSeatMan::Clear();
// CODEVIEW(E:\gamedcs\remote.h:197, dc 0x101dcc) void CHotSeatMan::AddPlayer(const char* sName);

// --- CMPEdit ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:263, dc 0x102014) void CMPEdit::CMPEdit(int textWidgetX, int textWidgetY, int textWidgetWidth, int textWidgetHeight, int textStringSize, char* textString, char* textFontName, int colorIndex, font::EJustify justification, char* backgroundIconName, int backgroundFrame, int textWidgetId, int textWidgetStyle, int iReadType, int textInsetX, int textInsetY);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:269, dc 0x1020b4) void CMPEdit::SetNextEdit(CMPEdit* pNextEdit);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:274, dc 0x1020bc) void CMPEdit::SetPrevEdit(CMPEdit* pPrevEdit);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:279, dc 0x1020c4) int CMPEdit::OnKeyPress(message* msg);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:309, dc 0x10215c) void CMPEdit::OnNextEdit();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:322, dc 0x102184) void CMPEdit::OnPrevEdit();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:335, dc 0x1021ac) void CMPEdit::SetFocus(unsigned char state);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:339, dc 0x1021c4) void* CMPEdit::`scalar deleting destructor'(unsigned __flags);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:339, dc 0x1021f8) void CMPEdit::~CMPEdit();

// --- CMPInputDlg ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:418, dc 0x1022f4) void CMPInputDlg::CMPInputDlg(int maxChars1, int maxChars2);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:465, dc 0x102718) void CMPInputDlg::~CMPInputDlg();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:470, dc 0x10275c) int CMPInputDlg::OnWidgetDeselect(int id, unsigned char* bExitFlag);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:493, dc 0x1027b4) unsigned char CMPInputDlg::OnOK();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:505, dc 0x1027ec) textWidget* CMPInputDlg::GetRolloverWidget();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:508, dc 0x1027f4) void CMPInputDlg::UpdateOK();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:521, dc 0x10286c) void CMPInputDlg::DisableOK();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:523, dc 0x102890) void* CMPInputDlg::`scalar deleting destructor'(unsigned __flags);

// --- CMPInputEdit ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:527, dc 0xffac0) int CMPInputEdit::OnKeyPress(message* msg);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:383, dc 0x102210) void CMPInputEdit::CMPInputEdit(int textWidgetX, int textWidgetY, int textWidgetWidth, int textWidgetHeight, int textStringSize, char* textString, char* textFontName, int colorIndex, font::EJustify justification, char* backgroundIconName, int backgroundFrame, int textWidgetId, int textWidgetStyle, int iReadType, int textInsetX, int textInsetY);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:384, dc 0x1022a8) void* CMPInputEdit::`scalar deleting destructor'(unsigned __flags);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:384, dc 0x1022dc) void CMPInputEdit::~CMPInputEdit();

// --- CMultiPlayerWindowEdit ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:141, dc 0x101e00) void CMultiPlayerWindowEdit::CMultiPlayerWindowEdit(int textWidgetX, int textWidgetY, int textWidgetWidth, int textWidgetHeight, int textStringSize, char* textString, char* textFontName, int colorIndex, font::EJustify justification, char* backgroundIconName, int backgroundFrame, int textWidgetId, int textWidgetStyle, int iReadType, int textInsetX, int textInsetY);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:145, dc 0x101e98) int CMultiPlayerWindowEdit::OnKeyPress(message* msg);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:156, dc 0x101ee8) void* CMultiPlayerWindowEdit::`scalar deleting destructor'(unsigned __flags);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:156, dc 0x101f1c) void CMultiPlayerWindowEdit::~CMultiPlayerWindowEdit();

// --- TMultiPlayerWindow ---
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:889, dc 0xffb70) void TMultiPlayerWindow::TMultiPlayerWindow();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1012, dc 0x100430) void TMultiPlayerWindow::~TMultiPlayerWindow();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1025, dc 0x100498) void TMultiPlayerWindow::GoSessionList();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1049, dc 0x10051c) void TMultiPlayerWindow::GoMainMenu();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1083, dc 0x1005fc) void TMultiPlayerWindow::Update();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1192, dc 0x1009a4) int TMultiPlayerWindow::OnWidgetDeselect(int id, unsigned char* bExitFlag);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1336, dc 0x100c1c) int TMultiPlayerWindow::WindowHandler(message* msg);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1344, dc 0x100c44) void TMultiPlayerWindow::RefreshSessions();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1359, dc 0x100ca0) unsigned char TMultiPlayerWindow::JoinSession(CDPlaySession* pSession, const char* password);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1378, dc 0x100d0c) unsigned char TMultiPlayerWindow::HostSession(const char* sessName, const char* password);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1420, dc 0x100e18) unsigned char TMultiPlayerWindow::InitRemote(eNetGameType netGameType, const char* sExtra, _DPCOMPORTADDRESS* comportInfo);
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1449, dc 0x100e94) void TMultiPlayerWindow::CheckSessions();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1461, dc 0x100ed0) unsigned char TMultiPlayerWindow::OnModemHost();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1495, dc 0x100f94) unsigned char TMultiPlayerWindow::OnDirectHost();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1540, dc 0x101058) unsigned char TMultiPlayerWindow::OnHost();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1588, dc 0x1011b8) unsigned char TMultiPlayerWindow::OnModemJoin();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1665, dc 0x101374) unsigned char TMultiPlayerWindow::OnDirectJoin();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1744, dc 0x101510) unsigned char TMultiPlayerWindow::OnJoin();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1884, dc 0x101784) unsigned char TMultiPlayerWindow::OnTCP();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1924, dc 0x1018ec) unsigned char TMultiPlayerWindow::OnIPX();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1944, dc 0x10196c) bool TMultiPlayerWindow::OnSearch();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:2009, dc 0x101c00) unsigned char TMultiPlayerWindow::OnHotSeat();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:2025, dc 0x101c4c) unsigned char TMultiPlayerWindow::OnModem();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:2043, dc 0x101ca4) unsigned char TMultiPlayerWindow::OnDirect();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:2061, dc 0x101cfc) unsigned char TMultiPlayerWindow::IsNT();
// CODEVIEW(E:\gamedcs\MultiPlayerWindow.h:91, dc 0x101da0) textWidget* TMultiPlayerWindow::GetRolloverWidget();
// CODEVIEW(E:\gamedcs\multiplayerwindow.cpp:1000, dc 0x102f98) void* TMultiPlayerWindow::`scalar deleting destructor'(unsigned __flags);

#endif  /* HOMM3_MULTIPLAYERWINDOW_H */
