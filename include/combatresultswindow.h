// combatresultswindow.h - prototypes of combatresultswindow.cpp (compiland combatresultswindow.obj)
// HAND-OWNED after admission - INITIALLY GENERATED by `python3 -m homm3.carve carcass`.
// Class layouts are NOT fabricated from method symbols;
// prototypes stay comments until a retail layout is proven.
#ifndef HOMM3_COMBATRESULTSWINDOW_H
#define HOMM3_COMBATRESULTSWINDOW_H

#include "window.h"

class hero;
class message;

// Retail's constructor initializes heroWindow directly, installs vtable
// 0x63d46c, and adds no derived storage. The nine slots reproduce the
// heroWindow roster, with this class overriding dtor/Open/Close only.
class TCombatResultsWindow : public heroWindow {
public:
    TCombatResultsWindow(const hero* attacker, const hero* defender,
        int my_side, int winning_side, unsigned char is_siege,
        int experience);
    virtual ~TCombatResultsWindow();
    virtual int Open(int newPriority, unsigned char update);
    virtual void Close(unsigned char update);
    void DoModal();

    // Dreamcast TCombatResultsWindow::EOtherWidgetIDs.
    //
    // CORRECTION (retail bytes, constructor 0x4702d0): only 200..207 are
    // reached. Retail confirms BACKGROUND_ID..RESULTS one for one - the
    // CPResult background, the attacker/defender name texts (201/204), the
    // TwCrPort portrait pair (202/205), the two status texts (203/206) and
    // the results text (207) are pushed with exactly those ids. The
    // fourteen ATTACKER_LOSS_*/DEFENDER_LOSS_* rows are NOT: every
    // per-creature loss icon and count text the loss loop emits carries
    // BACKGROUND_ID, because Complete builds them from a data-driven
    // aggregation loop rather than from fourteen fixed slots. The 208..221
    // half of this roster is Dreamcast-only and must not be used to name a
    // retail widget.
    enum EOtherWidgetIDs {
        BACKGROUND_ID = 200,
        ATTACKER_NAME = 201,
        ATTACKER_PORTRAIT = 202,
        ATTACKER_STATUS = 203,
        DEFENDER_NAME = 204,
        DEFENDER_PORTRAIT = 205,
        DEFENDER_STATUS = 206,
        RESULTS = 207,
        ATTACKER_LOSS_1 = 208,
        ATTACKER_LOSS_2 = 209,
        ATTACKER_LOSS_3 = 210,
        ATTACKER_LOSS_4 = 211,
        ATTACKER_LOSS_5 = 212,
        ATTACKER_LOSS_6 = 213,
        ATTACKER_LOSS_7 = 214,
        DEFENDER_LOSS_1 = 215,
        DEFENDER_LOSS_2 = 216,
        DEFENDER_LOSS_3 = 217,
        DEFENDER_LOSS_4 = 218,
        DEFENDER_LOSS_5 = 219,
        DEFENDER_LOSS_6 = 220,
        DEFENDER_LOSS_7 = 221,
        NWIDGETS = 21
    };
};
SIZE(TCombatResultsWindow, 0x4c);

// Retail .bss 0x695014, declared here for want of a proven owner: the
// constructor below writes it eight times, always paired with the video id it
// hands VideoOpen, and the ONLY other site in the image that touches it is the
// still-unclaimed 0x477470 (config/retail-reloc-evidence.tsv lists exactly ten
// references, eight of them ours). No DATA claim is made - this TU does not
// define it - and the declaration moves to the real owner's header as soon as
// 0x477470's compiland is located.
extern int gCombatResultFlag695014;
// Retail .data 0x6701a8, the six MP3 names the flag above selects,
// READ FROM THE IMAGE in order: "win battle", "losecombat",
// "defend castle", "retreat battle", "surrender battle", "losecastle" -
// which is exactly the 0..5 domain this file's own body assigns. The
// only proven reader is command.obj's DoVictory (0x477470), which loads
// `[4*gCombatResultFlag695014 + 0x6701a8]` straight into
// soundManager::StartMP3; declared here beside its index rather than in
// command.h because the two are one datum. DECLARATION ONLY - no TU in
// this tree defines it yet, so no DATA claim.
extern const char* const gCombatResultMusic[6];

// Retail /Gr passes the message in ECX, matching DoDialog's callback shape.
int CombatResultsWindowHandler(message& msg);

// --- TCombatResultsWindow ---
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:72, dc 0x68364) void TCombatResultsWindow::TCombatResultsWindow(const hero* attacker, const hero* defender, int my_side, int winning_side, unsigned char is_siege, int experience);
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:347, dc 0x69198) void TCombatResultsWindow::~TCombatResultsWindow();
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:361, dc 0x69200) int TCombatResultsWindow::Open(int newPriority, unsigned char update);
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:379, dc 0x69244) void TCombatResultsWindow::Close(unsigned char update);
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:392, dc 0x69268) void TCombatResultsWindow::DoModal();
// CODEVIEW(E:\gamedcs\combatresultswindow.cpp:334, dc 0x693a4) void* TCombatResultsWindow::`scalar deleting destructor'(unsigned __flags);

#endif  /* HOMM3_COMBATRESULTSWINDOW_H */
