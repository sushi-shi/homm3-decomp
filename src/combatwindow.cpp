// combatwindow.cpp - E:\gamedcs\combatwindow.cpp (compiland combatwindow.obj)
// HAND-OWNED after admission - INITIALLY GENERATED by `python3 -m homm3.carve carcass`; prototypes are Dreamcast CodeView evidence.
// 72 functions in link order; 20 compiler-generated $-thunks omitted.
#include <va.h>
#define HOMM3_COMBATWINDOW_END_PLACEMENT_VIEW
#include "combatwindow.h"
#include "combatwindowchatedit.h"
#include "cmbtmgr.h"
#include "border.h"
#include "combatcontrolsubwindow.h"
#include "font.h"
#include "game.h"
#include "cheatcode.h"
#include "inputmgr.h"
#include "kb.h"
#include "kbwin.h"
#include "message.h"
#include "remote.h"
#include "subwindow.h"
#include "textntry.h"
#include "textresource.h"
#include "textwdgt.h"
#include "winmgr.h"
#include "widget.h"

// Retail .bss 0x695000. The constructor publishes itself here for the chat
// edit callbacks and the destructor clears the slot. The Dreamcast image has
// the corresponding compiland-local pointer at 0x1bf12c.
static TCombatWindow* gpCombatWindow;

// E:\gamedcs\combatwindow.cpp:42. SendChat is the sole retail caller;
// Dreamcast supplies the reference ABI, local TCheatCode and statement map.
// The 28-block CFG is retail-identical. The 90.2966% residual is one measured
// VC6 inline decision: predict-inline reports that only _Tidy(false) expands
// here while retail calls it; direct operator=/assign/count/wrapper spellings
// are byte-flat, so keep the natural source rather than model string internals.
// E:\gamedcs\combatwindow.cpp:42
VA(0x00472010, 0x1C0)
void CheckCombatCheatCode(std::string& chatString)
{
    hero* currentHero =
        gpCombatManager->heroes[gpCombatManager->currentSide];
    std::string* chat = &chatString;
    TCheatCode code(chat->c_str());

    if (code.compare(DATA_COMPGEN(
            0x0063d490, combatCheatBluePill, "ajpoyhrcvyy"))) {
        gpCombatManager->Unnamed4693a0(gpCombatManager->currentSide);
    } else if (code.compare(DATA_COMPGEN(
                   0x0063d49c, combatCheatRedPill, "ajperqcvyy"))) {
        gpCombatManager->Unnamed4693a0(1 - gpCombatManager->currentSide);
    } else if (code.compare(DATA_COMPGEN(
                   0x0063d4a8, combatCheatAllSpells,
                   "ajpgurervfabfcbba"))
               && currentHero) {
        currentHero->mana = 999;
        if (!currentHero->IsWieldingArtifact(ARTIFACT_SPELLBOOK)) {
            type_artifact spellbook(ARTIFACT_SPELLBOOK, -1);
            currentHero->GiveArtifact(&spellbook, 1, 1);
        }
        for (int spell = 0; spell < hero::NUM_SPELLS; spell++)
            currentHero->AddSpell(spell);
    } else {
        return;
    }

    *chat = (*gpGeneralText)[261];
    gpGame->field_1f69c = 1;
    if (gbUnk69774c)
        gpGame->campaign.isCheater = 1;
}

// Retail inlines this compiland-private forwarding constructor into its sole
// use below: the base call is followed by the +0x70 clear and 0x63d4bc vptr.
inline CCombatChatEdit::CCombatChatEdit(
    int x, int y, int w, int h, int textSize, char* text, char* fontName,
    font::TColor color, font::EJustify justification, char* backgroundIcon,
    int backgroundFrame, int id, int style, int readType, int insetX,
    int insetY)
    : CChatEdit(x, y, w, h, textSize, text, fontName, color, justification,
                backgroundIcon, backgroundFrame, id, style, readType,
                insetX, insetY),
      field_70(0)
{
}

// E:\gamedcs\combatwindow.cpp:221. The sole retail caller,
// combatManager::Open, supplies the placement flag. Retail fixes every
// allocation extent, widget argument, panel coordinate, and registration
// order; Dreamcast independently supplies the constructor identity and call
// graph.
// E:\gamedcs\combatwindow.cpp:221
VA(0x004721d0, 0x42A)  // body + sole caller + medfont.fnt, dc 0x69850
TCombatWindow::TCombatWindow(unsigned char do_placement)
    : heroWindow(0, 0, 800, 600, 1)
{
    gpCombatWindow = this;
    chatWidget = 0;
    chatEdit = 0;

    Widgets.reserve(3);
    Widgets.push_back(new border(0, 0, 800, 556, 0, 1));

    chatWidget = new textWidget(
        75, 100, 520, 440, 0,
        DATA_COMPGEN(0x0065f2ec, combatWindowMedfont, "medfont.fnt"),
        font::CHAT, 1, font::BOTTOM_JUSTIFIED, 0, 8);
    chatEdit = new CCombatChatEdit(
        214, 563, 400, 32, 127,
        DATA_COMPGEN(0x00691210, combatChatEmptyText, ""),
        DATA_COMPGEN(0x0065f2f8, combatChatSmallFont, "smalfont.fnt"),
        font::WHITE, font::LEFT_JUSTIFIED,
        DATA_COMPGEN(0x00670020, combatChatBackground, "cRollovr.pcx"),
        0, 2, 0x100, textEntryWidget::READ_TYPE_INSET, 3, 0);
    Widgets.push_back(chatEdit);
    Widgets.push_back(chatWidget);

    for (std::vector<widget*>::iterator it = Widgets.begin();
         it != Widgets.end(); ++it) {
        if (*it)
            AddWidget(*it, -1);
        else
            MemError();
    }

    if (do_placement) {
        controlSubWindow = new TCombatPlacementSubWindow(this);
        WidgetSetStatus(COMBAT_RIGHT_COMMAND_0_ID, 8);
        WidgetSetStatus(COMBAT_RIGHT_COMMAND_1_ID, 8);
        WidgetSetStatus(COMBAT_RIGHT_COMMAND_2_ID, 8);
    } else {
        controlSubWindow = new TCombatControlSubWindow(this);
    }

    heroSubWindows[0] = new TCombatHeroSubWindow(1, 135, 78, 202, this);
    heroSubWindows[1] = new TCombatHeroSubWindow(721, 135, 78, 202, this);
    creatureSubWindows[0] =
        new TCombatCreatureSubWindow(1, 267, 78, 288, this, 1);
    creatureSubWindows[1] =
        new TCombatCreatureSubWindow(721, 267, 78, 288, this, 1);
    creatureSubWindows[2] =
        new TCombatCreatureSubWindow(1, 429, 78, 126, this, 2);
    creatureSubWindows[3] =
        new TCombatCreatureSubWindow(721, 429, 78, 126, this, 2);

    combatMessageCount = 0;
    combatMessageStart = 0;
}

// Vtable 0x63d4bc slot 15. The inactive editor consumes only the activation
// key; once active it delegates the complete editor key path to CChatEdit.
// E:\gamedcs\combatwindow.cpp:143
VA(0x00472600, 0xA5)  // vtable slot + activation-state body, dc 0x6a484
int CCombatChatEdit::OnKeyPress(message* msg)
{
    if (field_70)
        return CChatEdit::OnKeyPress(msg);

    if (GetCharPressed(msg) == KEYCODE_TAB) {
        if (gpCombatWindow->controlSubWindow
            && gpCombatWindow->controlSubWindow->rolloverWidget) {
            gpCombatWindow->controlSubWindow->rolloverWidget->send_message(
                widget::WIDGET_CLEAR_STATUS, widget::WIDGET_CLEAR_STATUS);
        }
        field_70 = 1;
        SetFocus(1);
        parentWindow->SetFocus(id);
        Draw();
        gpWindowManager->UpdateScreen(
            x + parentWindow->x, y + parentWindow->y, width, height);
        return 1;
    }
    return 0;
}

// Vtable 0x63d4bc slot 24. Local combat accepts cheat text before the common
// network/local chat path; both modes then close the editor and restore the
// combat control bar.
// E:\gamedcs\combatwindow.cpp:171
VA(0x004726b0, 0x131)  // vtable slot + SendChat/IsMultiplayer, dc 0x6a488
void CCombatChatEdit::SendChat(const char* sChat, int toWho)
{
    std::string chatString(sChat);
    if (!gpGame->IsMultiplayer())
        CheckCombatCheatCode(chatString);

    field_70 = 0;
    ::SendChat(chatString.c_str(), toWho);
    parentWindow->SetFocus(-1);
    SetFocus(0);

    type_combat_sub_window*& combatSubWindow =
        gpCombatWindow->controlSubWindow;
    if (combatSubWindow) {
        if (combatSubWindow->rolloverWidget) {
            combatSubWindow->rolloverWidget->send_message(
                widget::WIDGET_SET_STATUS, widget::WIDGET_CLEAR_STATUS);
        }
        combatSubWindow->Draw(1, -0xffff, 0xffff);
    }
}

// Vtable 0x63d4bc slot 21. Escape closes the edit without sending, redraws
// its rectangle, and restores the control-bar rollover line.
// E:\gamedcs\combatwindow.cpp:193
VA(0x004727f0, 0x5E)  // vtable slot + control-bar redraw, dc 0x6a540
int CCombatChatEdit::OnEscape(message msg)
{
    field_70 = 0;
    parentWindow->SetFocus(-1);
    SetFocus(0);
    Draw();

    type_combat_sub_window*& combatSubWindow =
        gpCombatWindow->controlSubWindow;
    if (combatSubWindow) {
        if (combatSubWindow->rolloverWidget) {
            combatSubWindow->rolloverWidget->send_message(
                widget::WIDGET_SET_STATUS, widget::WIDGET_CLEAR_STATUS);
        }
        combatSubWindow->Draw(1, -0xffff, 0xffff);
    }
    return 1;
}

// Vtable 0x63d4bc slot 19. The inactive editor deliberately performs no
// drawing; activation makes this the ordinary draw-and-post rectangle path.
// E:\gamedcs\combatwindow.cpp:207
VA(0x00472850, 0x3D)  // vtable slot + field_70 guard, dc 0x6a590
void CCombatChatEdit::UpdateScreen()
{
    if (field_70) {
        Draw();
        gpWindowManager->UpdateScreen(
            x + parentWindow->x, y + parentWindow->y, width, height);
    }
}

// Vtable 0x63d4bc slot 0. This must remain implicit: an explicit empty
// body makes VC6 insert a derived-vtable store before the base destructor.
VA_COMPGEN(0x00472890, 0x05, IMPLICIT_DTOR, CCombatChatEdit)

// Vtable 0x63d528 slot 0.
VA_COMPGEN(0x004728a0, 0x21, SCALAR_DELETING_DTOR, TCombatWindow)

// E:\gamedcs\combatwindow.cpp:281
VA(0x004728d0, 0x2A)  // vtable 0x63d528 slot 2 + heroWindow::Close, dc 0x69b2c
void TCombatWindow::Close(unsigned char update)
{
    if (controlSubWindow) {
        delete controlSubWindow;
        controlSubWindow = 0;
    }
    heroWindow::Close(update);
}

// EXACT: E:\gamedcs\combatwindow.cpp:293-313. Retail extends the DC teardown with
// four additional creature panels, but preserves its three source phases:
// destroy the control bar and inherited widgets, destroy every owned message,
// then release the side panels and clear the callback-visible singleton.
VA(0x00472900, 0x14E)
TCombatWindow::~TCombatWindow()
{
    if (controlSubWindow)
        delete controlSubWindow;

    for (std::vector<widget*>::iterator it = Widgets.begin();
         it != Widgets.end(); ++it)
        delete *it;

    for (unsigned int i = 0; i < combatMessages.size(); ++i)
        delete combatMessages[i];

    delete heroSubWindows[0];
    delete heroSubWindows[1];
    delete creatureSubWindows[0];
    delete creatureSubWindows[1];
    delete creatureSubWindows[2];
    delete creatureSubWindows[3];

    gpCombatWindow = 0;
}

// Retail folds the DC helper at combatwindow.cpp:322-346 into its only caller.
// The two scroll arrows deliberately share help row 5.
inline int TCombatWindow::convertID2HelpID(int id)
{
    if (id < 0)
        return -1;

    switch (id) {
    case COMBAT_LEFT_COMMAND_0_ID: return 0;
    case COMBAT_LEFT_COMMAND_1_ID: return 1;
    case COMBAT_LEFT_COMMAND_2_ID: return 2;
    case COMBAT_LEFT_COMMAND_3_ID: return 3;
    case COMBAT_ROLLOVER_ID: return 4;
    case COMBAT_LOG_SCROLL_UP_ID:
    case COMBAT_LOG_SCROLL_DOWN_ID: return 5;
    case COMBAT_RIGHT_COMMAND_0_ID: return 6;
    case COMBAT_RIGHT_COMMAND_1_ID: return 7;
    case COMBAT_RIGHT_COMMAND_2_ID: return 8;
    case COMBAT_PLACEMENT_COMMAND_0_ID: return 9;
    case COMBAT_PLACEMENT_COMMAND_1_ID: return 10;
    default: return -1;
    }
}

// EXACT: E:\gamedcs\combatwindow.cpp:356-374. Retail inlines convertID2HelpID,
// selects the right-click column of the shared eleven-row help table, sizes
// the text, and centers a type-4 dialog in the 800x600 combat window.
VA(0x00472a50, 0x124)
unsigned char TCombatWindow::ProcessRightSelect(const message* msg)
{
    int helpID = convertID2HelpID(msg->codeY);
    if (helpID < 0)
        return 0;

    const char* text = gCombatSubWindowHelp[helpID].rclick;
    int width;
    int height;
    CalculateDialogTextSize(text, &width, &height);
    NormalDialog(text, 4, (WINDOW_SCREEN_WIDTH - width) / 2,
        (WINDOW_SCREEN_HEIGHT - height) / 2,
        -1, 0, -1, 0, -1, 0, -1, 0);
    return 1;
}

// Dreamcast keeps this source helper out of line; retail /Ob2 folds both call
// sites in handle_widget_hover. The control-bar vtable and the chat editor's
// +0x6d focus byte independently prove the two member types.
inline void TCombatWindow::set_rollover(const char* new_text)
{
    if (controlSubWindow && !chatEdit->bHasFocus)
        controlSubWindow->set_rollover(new_text);
}

// EXACT: E:\gamedcs\combatwindow.cpp:393-407. Retail vtable 0x63d528 slot 4
// points here; the DC source-line blocks preserve the accessor, early-return
// condition, and the null/non-null rollover arms.
VA(0x00472b80, 0x67)
void TCombatWindow::handle_widget_hover(widget* current_widget)
{
    const char* new_text = current_widget->RollOver;
    if (combatMessageCount > 0
        && (!new_text || current_widget->id == COMBAT_LOG_SCROLL_UP_ID
            || current_widget->id == COMBAT_LOG_SCROLL_DOWN_ID))
        return;

    if (!new_text)
        set_rollover(DATA_COMPGEN(
            0x00691210, combatHoverRolloverEmptyText, ""));
    else
        set_rollover(new_text);
}

// EXACT: the timed message clear is the retail 0x472bf0 body.  The Dreamcast row
// places ClearCombatMessages between handle_widget_hover and show_messages;
// retail's sole combatManager::RightClick call, the +0x64 message count and
// +0x6c GameTime stamp, and its call to combat_message prove the identity.
VA(0x00472bf0, 0x36)
void TCombatWindow::ClearCombatMessages()
{
    if (combatMessageCount
        && GameTime::ElapsedSince(combatMessageTime) >= 3000) {
        combatMessageCount = 0;
        combat_message(
            DATA_COMPGEN(0x00691210, combatRolloverEmptyText, ""), 0, 0);
    }
}

// EXACT: E:\gamedcs\combatwindow.cpp:431-450. The DC line records preserve the
// two-message string assembly and the control-bar range update; retail fixes
// the message-vector offsets, the +0x68 selected row, and the +0x6c clock.
VA(0x00472c30, 0x173)
void TCombatWindow::show_messages(long start)
{
    if (start < combatMessages.size()) {
        std::string result;
        combatMessageStart = start;
        result = *combatMessages[start];
        if (start + 1 < combatMessages.size()) {
            result += '\n';
            result += *combatMessages[start + 1];
        }
        if (controlSubWindow)
            controlSubWindow->set_rollover_buttons(
                start, combatMessages.size());
        set_rollover(result.c_str());
        combatMessageTime = GameTime::Get();
    }
}

// EXACT: E:\gamedcs\combatwindow.cpp:456-469. The control-bar guard and the
// +0x68 current line establish the retail row; DC's source lines preserve
// the size-minus-two upper clamp, zero lower clamp, and show_messages call.
VA(0x00472db0, 0x40)
void TCombatWindow::scroll_rollover(long delta)
{
    if (controlSubWindow) {
        long start = combatMessageStart;
        long last = combatMessages.size() - 2;
        start += delta;
        if (start > last)
            start = last;
        if (start < 0)
            start = 0;
        show_messages(start);
    }
}

// EXACT: E:\gamedcs\combatwindow.cpp:508-574. The DC statement map preserves the
// transient-versus-kept arms, newline split, two-line cap, and final visible
// range. Retail fixes the live combat-manager guards and the +0x54 message
// vector shared with show_messages and the destructor.
VA(0x00472e90, 0x35E)
void TCombatWindow::combat_message(const char* new_text,
                                   unsigned char keep,
                                   unsigned char priority)
{
    if (gpCombatManager->IsQuickCombat())
        return;
    if (!controlSubWindow->rolloverWidget)
        return;
    if (!gpCombatManager->field_13300)
        return;
    if (gpCombatManager->field_132f8)
        return;

    if (!keep) {
        if (combatMessageCount > 0
                && GameTime::ElapsedSince(combatMessageTime) < 3000
                && !priority)
            return;
        set_rollover(new_text);
        return;
    }

    std::string cTemp(new_text);
    unsigned int split = cTemp.find('\n');
    combatMessageTime = GameTime::Get();
    combatMessageCount++;

    if (split == std::string::npos) {
        combatMessages.push_back(new std::string(cTemp));
    } else {
        cTemp[split] = ' ';
        if (gUnnamed698a08->LineLength(
                cTemp.c_str(), controlSubWindow->rolloverWidget->width) < 2) {
            combatMessages.push_back(new std::string(cTemp));
        } else {
            combatMessageCount++;
            combatMessages.push_back(new std::string(cTemp.substr(0, split)));
            combatMessages.push_back(new std::string(
                cTemp.substr(split + 1, std::string::npos)));
        }
    }

    if (combatMessageCount > 2)
        combatMessageCount = 2;
    if (!controlSubWindow)
        return;
    show_messages(combatMessages.size() - combatMessageCount);
}

// EXACT: E:\gamedcs\combatwindow.cpp:583-594. The DC statement map and retail both
// replace the placement bar with the ordinary control bar, redraw the entire
// window through vslot 5, and post the full 800x600 screen.
VA(0x004731f0, 0x99)
void TCombatWindow::EndPlacementPhase()
{
    if (controlSubWindow) {
        delete controlSubWindow;
        controlSubWindow = 0;
    }
    controlSubWindow = new TCombatControlSubWindow(this);
    DrawWindow(1, WINDOW_ALL_WIDGETS_LOW, WINDOW_ALL_WIDGETS_HIGH);
    gpWindowManager->UpdateScreen(
        0, 0, WINDOW_SCREEN_WIDTH, WINDOW_SCREEN_HEIGHT);
}

// The combat chat painter is the retail 0x473290 body: refresh the twenty-line
// text widget from chatMan, draw it, and optionally post exactly its rectangle.
// E:\gamedcs\combatwindow.cpp:603
VA(0x00473290, 0x52)  // DrawFrame caller + DC callee set, dc 0x6a264
void TCombatWindow::DrawChatText(unsigned char update)
{
    if (chatWidget) {
        chatMan.UpdateWidget(chatWidget, 1, 20);
        chatWidget->Draw();
        if (update) {
            gpWindowManager->UpdateScreen(
                chatWidget->x, chatWidget->y,
                chatWidget->width, chatWidget->height);
        }
    }
}

// EXACT: E:\gamedcs\combatwindow.cpp:652-655. Retail vtable 0x63d528 slot 5
// proves the identity. VC6 inlines DrawChatEdit: draw the focused editor and,
// when requested, post exactly its widget rectangle after the base painter.
VA(0x004732f0, 0x59)
void TCombatWindow::DrawWindow(unsigned char update, int low, int high)
{
    heroWindow::DrawWindow(update, low, high);
    if (chatEdit && chatEdit->bHasFocus) {
        chatEdit->Draw();
        if (update) {
            gpWindowManager->UpdateScreen(
                chatEdit->x, chatEdit->y,
                chatEdit->width, chatEdit->height);
        }
    }
}

#if 0  // @carcass: remaining combat-window bodies are not reconstructed yet

// E:\gamedcs\combatwindow.cpp:382
DC_ONLY(0x69d5c, 0x4C)
void TCombatWindow::set_rollover(const char* new_text)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:393
DC_ONLY(0x69da8, 0x56)
void TCombatWindow::handle_widget_hover(widget* current_widget)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:417
DC_ONLY(0x69e00, 0x3A)
void TCombatWindow::ClearCombatMessages()
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:431
DC_ONLY(0x69e3c, 0xE8)
void TCombatWindow::show_messages(long start)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:456
DC_ONLY(0x69f24, 0x46)
void TCombatWindow::scroll_rollover(long delta)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:475
DC_ONLY(0x69f6c, 0x26)
int TCombatWindow::scroll_up(message* msg)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:491
DC_ONLY(0x69f94, 0x26)
int TCombatWindow::scroll_down(message* msg)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:583
DC_ONLY(0x6a200, 0x62)
void TCombatWindow::EndPlacementPhase()
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:603
DC_ONLY(0x6a264, 0x5C)
void TCombatWindow::DrawChatText(unsigned char update)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:615
DC_ONLY(0x6a2c0, 0x50)
void TCombatWindow::DrawChatEdit(unsigned char update)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:633
DC_ONLY(0x6a310, 0xB0)
void TCombatWindow::OnChatActivate(unsigned char active)
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:652
DC_ONLY(0x6a3c0, 0x28)
void TCombatWindow::DrawWindow(unsigned char update, int low, int high)
{
    // @stub
}

// E:\gamedcs\widget.h:231
DC_ONLY(0x6a3e8, 0x4)
const char* widget::get_help_text()
{
    // @stub
}

// E:\gamedcs\combatwindow.cpp:313
DC_ONLY(0x6a65c, 0x34)
void* std::basic_string<char,std::char_traits<char>,std::allocator<char> >::`scalar deleting destructor'(unsigned __flags)
{
    // @stub
}

// ..\stlport\stl_string.h:563
DC_ONLY(0x6a690, 0x6)
char* std::basic_string<char,std::char_traits<char>,std::allocator<char> >::operator[](unsigned __n)
{
    // @stub
}

// ..\stlport\stl_string.h:1259
DC_ONLY(0x6a698, 0x8C)
std::basic_string<char,std::char_traits<char>,std::allocator<char> std::basic_string<char,std::char_traits<char>,std::allocator<char> >::substr(__$ReturnUdt, unsigned __pos, unsigned __n)
{
    // @stub
}

// ..\stlport\stl_vector.h:195
DC_ONLY(0x6a724, 0xC)
unsigned std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *> >::size()
{
    // @stub
}

// ..\stlport\stl_vector.h:203
DC_ONLY(0x6a730, 0x20)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *> >::operator[](unsigned __n)
{
    // @stub
}

// ..\stlport\stl_vector.h:218
DC_ONLY(0x6a750, 0x1C)
void std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,(const std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char>* __a)
{
    // @stub
}

// ..\stlport\stl_vector.h:288
DC_ONLY(0x6a76c, 0x28)
void std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,s()
{
    // @stub
}

// ..\stlport\stl_vector.h:368
DC_ONLY(0x6a794, 0x3C)
void std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *> >::push_back(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __x)
{
    // @stub
}

// ..\stlport\stl_alloc.h:527
DC_ONLY(0x6a7d0, 0x4)
void std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>()
{
    // @stub
}

// ..\stlport\stl_alloc.h:537
DC_ONLY(0x6a7d4, 0x4)
void std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>::~allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>()
{
    // @stub
}

// ..\stlport\stl_string.h:364
DC_ONLY(0x6a7d8, 0x38)
void std::basic_string<char,std::char_traits<char>,std::allocator<char> >::basic_string<char,std::char_traits<char>,std::allocator<char> >(const char* __f, const char* __l, const std::allocator<char>* __a)
{
    // @stub
}

// ..\stlport\stl_vector.h:179
DC_ONLY(0x6a810, 0x4)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *> >::begin()
{
    // @stub
}

// ..\stlport\stl_vector.h:89
DC_ONLY(0x6a814, 0x2C)
void std::_Vector_base<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::a(const std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char>* __a)
{
    // @stub
}

// ..\stlport\stl_vector.h:101
DC_ONLY(0x6a840, 0x30)
void std::_Vector_base<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::al()
{
    // @stub
}

// ..\stlport\stl_string.h:469
DC_ONLY(0x6a870, 0x18)
void std::_STL_alloc_proxy<std::basic_string<char,std::char_traits<char>,std::allocator<char> > * *,std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> >()
{
    // @stub
}

// ..\stlport\stl_alloc.h:1004
DC_ONLY(0x6a888, 0xC)
void std::_STL_alloc_proxy<std::basic_string<char,std::char_traits<char>,std::allocator<char> > * *,std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> >(const std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char>* __a, std::basic_string<char,std::char_traits<char>,std::allocator<char>*** __p)
{
    // @stub
}

// ..\stlport\stl_alloc.h:1025
DC_ONLY(0x6a894, 0x2C)
void std::_STL_(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __p, unsigned __n)
{
    // @stub
}

// ..\stlport\stl_alloc.h:552
DC_ONLY(0x6a8c0, 0x1C)
void std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>::deallocate(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __p, unsigned __n)
{
    // @stub
}

// ..\stlport\stl_string.c:205
DC_ONLY(0x6a8dc, 0x4)
void std::_String_base<char,std::allocator<char> >::_M_throw_out_of_range()
{
    // @stub
}

// ..\stlport\stl_string.c:551
DC_ONLY(0x6a8e0, 0x78)
unsigned std::basic_string<char,std::char_traits<char>,std::allocator<char> >::find(char __c, unsigned __pos)
{
    // @stub
}

// ..\stlport\stl_vector.c:248
DC_ONLY(0x6a958, 0xCC)
void std::vector<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *,std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *> >::_M_insert_overflow(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __position, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __x, unsigned __fill_len)
{
    // @stub
}

// ..\stlport\stl_construct.h:128
DC_ONLY(0x6aa24, 0x30)
void std::destroy(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last)
{
    // @stub
}

// ..\stlport\stl_construct.h:85
DC_ONLY(0x6aa54, 0x28)
void std::construct(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __p, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __value)
{
    // @stub
}

// ..\stlport\stl_alloc.h:968
DC_ONLY(0x6aa7c, 0x4)
std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char>* std::__stl_alloc_rebind(std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char>* __a, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __formal)
{
    // @stub
}

// ..\stlport\stl_alloc.h:1022
DC_ONLY(0x6aa80, 0x28)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::_ST(unsigned __n)
{
    // @stub
}

// ..\stlport\stl_alloc.h:547
DC_ONLY(0x6aaa8, 0x24)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::allocator<std::basic_string<char,std::char_traits<char>,std::allocator<char> > *>::allocate(unsigned __n, const void* __formal)
{
    // @stub
}

// ..\stlport\stl_function.h:269
DC_ONLY(0x6aacc, 0x38)
std::binder2nd<std::_Eq_traits<std::char_traits<char> std::bind2nd(__$ReturnUdt, const std::_Eq_traits<std::char_traits<char>* __fn, const char* __x)
{
    // @stub
}

// ..\stlport\stl_algo.h:112
DC_ONLY(0x6ab04, 0x20)
const char* std::find_if(const char* __first, const char* __last, std::binder2nd<std::_Eq_traits<std::char_traits<char> __pred)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:97
DC_ONLY(0x6ab24, 0x38)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::uninitialized_copy(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __result)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:263
DC_ONLY(0x6ab5c, 0x38)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::uninitialized_fill_n(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, unsigned __n, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __x)
{
    // @stub
}

// ..\stlport\stl_iterator_base.h:262
DC_ONLY(0x6ab94, 0x4)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::value_type(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:121
DC_ONLY(0x6ab98, 0x1C)
void std::__destroy(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __formal)
{
    // @stub
}

// ..\stlport\stl_function.h:257
DC_ONLY(0x6abb4, 0x10)
void std::binder2nd<std::_Eq_traits<std::char_traits<char> > >::binder2nd<std::_Eq_traits<std::char_traits<char> > >(const std::_Eq_traits<std::char_traits<char>* __x, const char* __y)
{
    // @stub
}

// ..\stlport\stl_algo.h:77
DC_ONLY(0x6abc4, 0x38)
const char* std::find_if(const char* __first, const char* __last, std::binder2nd<std::_Eq_traits<std::char_traits<char> __pred, std::input_iterator_tag __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:88
DC_ONLY(0x6abfc, 0x1C)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::__uninitialized_copy(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __result, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:255
DC_ONLY(0x6ac18, 0x1C)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::__uninitialized_fill_n(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, unsigned __n, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __x, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:110
DC_ONLY(0x6ac34, 0x30)
void std::__destroy_aux(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_function.h:261
DC_ONLY(0x6ac64, 0x18)
unsigned char std::binder2nd<std::_Eq_traits<std::char_traits<char> > >::operator()(const char* __x)
{
    // @stub
}

// ..\stlport\stl_ctraits_fns.h:34
DC_ONLY(0x6ac7c, 0x18)
unsigned char std::_Eq_traits<std::char_traits<char> >::operator()(const char* __x, const char* __y)
{
    // @stub
}

// ..\stlport\char_traits.h:74
DC_ONLY(0x6ac94, 0xA)
unsigned char std::__char_traits_base<char,int>::eq(const char* __c1, const char* __c2)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:70
DC_ONLY(0x6aca0, 0x3C)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::__uninitialized_copy_aux(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __last, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __result, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:239
DC_ONLY(0x6acdc, 0x3C)
std::basic_string<char,std::char_traits<char>,std::allocator<char>** std::__uninitialized_fill_n_aux(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __first, unsigned __n, std::basic_string<char,std::char_traits<char>,std::allocator<char>** __x, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:59
DC_ONLY(0x6ad18, 0x1C)
void std::destroy(std::basic_string<char,std::char_traits<char>,std::allocator<char>** __pointer)
{
    // @stub
}

// ..\stlport\stl_construct.h:53
DC_ONLY(0x6ad34, 0x4)
void std::__destroy_aux()
{
    // @stub
}

#endif  // @carcass
