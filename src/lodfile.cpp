// lodfile.cpp - E:\gamedcs\lodfile.cpp (compiland lodfile.obj)
// HAND-OWNED after admission - INITIALLY GENERATED by `python3 -m homm3.carve carcass`; prototypes are Dreamcast CodeView evidence.
// 61 functions in link order.
//
// Retail contribution: 0xfa590..0xfaeab. The TU head is LODFile::clear at
//   0xfa590 (NOT 0xfa610 as first bracketed): levelupwindow's flank ends with
//   two 8-byte point helpers 0xfa520/0xfa540 and the atexit thunk 0xfa570
//   (registers border_vslot10 - none touch the LODFile layout). The tail is
//   the STL-COMDAT row 0xfac40 (0x26b, vector<LODEntry> fill-insert, 32-byte
//   stride, sole caller LODFile::open - excluded class, stays DC_ONLY);
//   mainmenu's terrain.h $E head follows at 0xfaeb0.
// 9 extern spine rows have no retail slot (every inter-claim gap is closed):
//   GetFileSize, getDataPtr, exist, getErrorString, set_filemap, sort -
//     unreferenced by retail game code, /OPT:REF-stripped (the clear/flush
//     logic of set_filemap survives only inlined in clear/open/~LODFile);
//   compare - address only taken by sort, stripped with it (retail open has
//     no qsort call);
//   LODEntry::LODEntry - trivial, absorbed into the 0xfac40 COMDAT;
//   LODHeader::LODHeader - visibly inlined in LODFile::LODFile at 0xfa780
//     ("LOD" strcpy into this+0x11c, +0x120=500, 20 dwords zeroed).
#include <va.h>
#include <string.h>
#include "lodfile.h"

// E:\gamedcs\lodfile.cpp:38.  The TU head.  subindex.clear() is
// Dinkumware's erase(begin(), end()), which is what the zero-trip
// 32-byte copy loop is: it only ever stores back _Last = _First.
VA(0x004fa590, 0x77)  // order-map TU head, dc 0xe908c
void LODFile::clear()
{
    if (opened) {
        subindex.clear();
        fclose(fileptr);
        delete dataBuffer;
        opened = 0;
    }
}

#if 0  // @carcass

// E:\gamedcs\lodfile.cpp:53
DC_ONLY(0xe90c0, 0x3E)
int LODFile::GetFileSize()
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:72
DC_ONLY(0xe9100, 0x54)
void* LODFile::getDataPtr(const char* item_name)
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\lodfile.cpp:93
VA(0x004fa610, 0x45)  // anchor-global, dc 0xe9154
LODEntry* LODFile::getItemIndex(const char* item_name)
{
    if (opened) {
        Find(0, numEntries, item_name);
        if (matchindex >= 0)
            return &subindex[matchindex];
    }
    return 0;
}

// E:\gamedcs\lodfile.cpp:125.  A binary search that hands off to a
// linear scan once the live span drops to four or fewer rows.  Retail
// carries TWO copies of that scan, one per comparison arm, so the span
// test sits inside each arm rather than above the split.
//
// Residual (93.98%): instruction-for-instruction identical, four exit
// blocks against our three.  Retail's four tails are pairwise identical
// APART FROM one scratch register (the `matchindex = i` stores use ecx
// in the upper arm and edx in the lower, the two `matchindex = -1`
// blocks use eax and edx), so its tail-merger folded only the upper
// linear hit onto the midpoint hit; ours makes all four congruent and
// merges them into two.  `homm3 vc6 diagnose` calls it control-flow
// (21 vs 22 blocks, three retargeted branches, register-distance 150),
// but the block set is downstream of that register choice, so no local
// spelling reaches it.  Per-function fuzzy 74.09% - the objdiff move
// penalty over a long run of near-identical instructions, not a
// semantic gap.  Tried and rejected: an explicit `half` local, `mid`
// hoisted above the loop, `mid` recomputed per use, and both
// asymmetric exit forms that would give each linear scan its own
// `matchindex = -1` block (68.87% and 69.18%).  Same family as the
// documented merged-return residual.
VA(0x004fa660, 0x113)  // anchor-global, dc 0xe91c0
void LODFile::Find(unsigned begin, unsigned end, const char* item_name)
{
    while (begin != end) {
        unsigned span = end - begin;
        unsigned mid = begin + span / 2;
        int order = _strcmpi(item_name, subindex[mid].name);
        if (order == 0) {
            matchindex = mid;
            return;
        }
        if (order < 0) {
            if (span <= 4) {
                for (unsigned i = begin; i < end; i++) {
                    if (_strcmpi(item_name, subindex[i].name) == 0) {
                        matchindex = i;
                        return;
                    }
                }
                break;
            }
            end = mid;
        } else {
            if (span <= 4) {
                for (unsigned j = begin; j < end; j++) {
                    if (_strcmpi(item_name, subindex[j].name) == 0) {
                        matchindex = j;
                        return;
                    }
                }
                break;
            }
            begin = mid;
        }
    }
    matchindex = -1;
}

#if 0  // @carcass

// E:\gamedcs\lodfile.cpp:112
DC_ONLY(0xe9198, 0x26)
unsigned char LODFile::exist(const char* item_name)
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:125 - promoted to a live claim above.

// E:\gamedcs\lodfile.cpp:189
DC_ONLY(0xe92b4, 0x44)
char* LODFile::getErrorString(int LODErr)
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:226
DC_ONLY(0xe92f8, 0x38)
void LODEntry::LODEntry()
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:240 / 252 / 266 - promoted to live claims below.

// E:\gamedcs\lodfile.cpp:277
// RETAIL_LOCATED(0x004fa8a0, 0x1c4): not reconstructed; anchor-bracket, dc 0xe941c
int LODFile::open(const char* filename, int flags)
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:341
DC_ONLY(0xe955c, 0xF6)
void LODFile::set_filemap(unsigned char on)
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:393
DC_ONLY(0xe9654, 0x12)
int compare(const void* arg1, const void* arg2)
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:402
DC_ONLY(0xe9668, 0x28)
void LODFile::sort()
{
    // @stub
}

// E:\gamedcs\lodfile.cpp:430 - promoted to a live claim below.

// E:\gamedcs\lodfile.cpp:452 - promoted to a live claim below.

// ..\stlport\stl_vector.h:203
DC_ONLY(0xe9868, 0x20)
LODEntry* std::vector<LODEntry,std::allocator<LODEntry> >::operator[](unsigned __n)
{
    // @stub
}

// ..\stlport\stl_vector.h:218
DC_ONLY(0xe9888, 0x1C)
void std::vector<LODEntry,std::allocator<LODEntry> >::vector<LODEntry,std::allocator<LODEntry> >(const std::allocator<LODEntry>* __a)
{
    // @stub
}

// ..\stlport\stl_vector.h:288
DC_ONLY(0xe98a4, 0x28)
void std::vector<LODEntry,std::allocator<LODEntry> >::~vector<LODEntry,std::allocator<LODEntry> >()
{
    // @stub
}

// ..\stlport\stl_vector.h:505
DC_ONLY(0xe98cc, 0x30)
void std::vector<LODEntry,std::allocator<LODEntry> >::resize(unsigned __new_size)
{
    // @stub
}

// ..\stlport\stl_vector.h:506
DC_ONLY(0xe98fc, 0x38)
void std::vector<LODEntry,std::allocator<LODEntry> >::clear()
{
    // @stub
}

// ..\stlport\stl_alloc.h:527
DC_ONLY(0xe9934, 0x4)
void std::allocator<LODEntry>::allocator<LODEntry>()
{
    // @stub
}

// ..\stlport\stl_alloc.h:537
DC_ONLY(0xe9938, 0x4)
void std::allocator<LODEntry>::~allocator<LODEntry>()
{
    // @stub
}

// ..\stlport\stl_vector.h:179
DC_ONLY(0xe993c, 0x4)
LODEntry* std::vector<LODEntry,std::allocator<LODEntry> >::begin()
{
    // @stub
}

// ..\stlport\stl_vector.h:181
DC_ONLY(0xe9940, 0x4)
LODEntry* std::vector<LODEntry,std::allocator<LODEntry> >::end()
{
    // @stub
}

// ..\stlport\stl_vector.h:490
DC_ONLY(0xe9944, 0x3C)
LODEntry* std::vector<LODEntry,std::allocator<LODEntry> >::erase(LODEntry* __first, LODEntry* __last)
{
    // @stub
}

// ..\stlport\stl_vector.h:499
DC_ONLY(0xe9980, 0x7C)
void std::vector<LODEntry,std::allocator<LODEntry> >::resize(unsigned __new_size, const LODEntry* __x)
{
    // @stub
}

// ..\stlport\stl_vector.h:89
DC_ONLY(0xe99fc, 0x2C)
void std::_Vector_base<LODEntry,std::allocator<LODEntry> >::_Vector_base<LODEntry,std::allocator<LODEntry> >(const std::allocator<LODEntry>* __a)
{
    // @stub
}

// ..\stlport\stl_vector.h:101
DC_ONLY(0xe9a28, 0x30)
void std::_Vector_base<LODEntry,std::allocator<LODEntry> >::~_Vector_base<LODEntry,std::allocator<LODEntry> >()
{
    // @stub
}

// ..\stlport\stl_bvector.h:101
DC_ONLY(0xe9a58, 0x18)
void std::_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >::~_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >()
{
    // @stub
}

// ..\stlport\stl_vector.h:195
DC_ONLY(0xe9a70, 0xC)
unsigned std::vector<LODEntry,std::allocator<LODEntry> >::size()
{
    // @stub
}

// ..\stlport\stl_vector.h:472
DC_ONLY(0xe9a7c, 0x18)
void std::vector<LODEntry,std::allocator<LODEntry> >::insert(LODEntry* __pos, unsigned __n, const LODEntry* __x)
{
    // @stub
}

// ..\stlport\stl_alloc.h:1004
DC_ONLY(0xe9a94, 0xC)
void std::_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >::_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >(const std::allocator<LODEntry>* __a, LODEntry** __p)
{
    // @stub
}

// ..\stlport\stl_alloc.h:1025
DC_ONLY(0xe9aa0, 0x2C)
void std::_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >::deallocate(LODEntry* __p, unsigned __n)
{
    // @stub
}

// ..\stlport\stl_alloc.h:552
DC_ONLY(0xe9acc, 0x1C)
void std::allocator<LODEntry>::deallocate(LODEntry* __p, unsigned __n)
{
    // @stub
}

// ..\stlport\stl_vector.c:283
DC_ONLY(0xe9ae8, 0xF0)
void std::vector<LODEntry,std::allocator<LODEntry> >::_M_fill_insert(LODEntry* __position, unsigned __n, const LODEntry* __x)
{
    // @stub
}

// ..\stlport\stl_construct.h:128
DC_ONLY(0xe9bd8, 0x30)
void std::destroy(LODEntry* __first, LODEntry* __last)
{
    // @stub
}

// ..\stlport\stl_algobase.h:322
DC_ONLY(0xe9c08, 0x50)
LODEntry* std::copy(LODEntry* __first, LODEntry* __last, LODEntry* __result)
{
    // @stub
}

// ..\stlport\stl_alloc.h:968
DC_ONLY(0xe9c58, 0x4)
std::allocator<LODEntry>* std::__stl_alloc_rebind(std::allocator<LODEntry>* __a, const LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_vector.c:248
DC_ONLY(0xe9c5c, 0xD0)
void std::vector<LODEntry,std::allocator<LODEntry> >::_M_insert_overflow(LODEntry* __position, const LODEntry* __x, unsigned __fill_len)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:97
DC_ONLY(0xe9d2c, 0x38)
LODEntry* std::uninitialized_copy(LODEntry* __first, LODEntry* __last, LODEntry* __result)
{
    // @stub
}

// ..\stlport\stl_algobase.h:442
DC_ONLY(0xe9d64, 0x50)
LODEntry* std::copy_backward(LODEntry* __first, LODEntry* __last, LODEntry* __result)
{
    // @stub
}

// ..\stlport\stl_algobase.h:495
DC_ONLY(0xe9db4, 0x3E)
void std::fill(LODEntry* __first, LODEntry* __last, const LODEntry* __value)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:263
DC_ONLY(0xe9df4, 0x38)
LODEntry* std::uninitialized_fill_n(LODEntry* __first, unsigned __n, const LODEntry* __x)
{
    // @stub
}

// ..\stlport\stl_iterator_base.h:262
DC_ONLY(0xe9e2c, 0x4)
LODEntry* std::value_type(const LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:121
DC_ONLY(0xe9e30, 0x1C)
void std::__destroy(LODEntry* __first, LODEntry* __last, LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_iterator_base.h:243
DC_ONLY(0xe9e4c, 0xC)
std::random_access_iterator_tag std::iterator_category(__$ReturnUdt, const LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_iterator_base.h:291
DC_ONLY(0xe9e58, 0x4)
int* std::distance_type(const LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_algobase.h:209
DC_ONLY(0xe9e5c, 0x4C)
LODEntry* std::__copy(LODEntry* __first, LODEntry* __last, LODEntry* __result, std::random_access_iterator_tag __formal, int* __formal)
{
    // @stub
}

// ..\stlport\stl_alloc.h:1022
DC_ONLY(0xe9ea8, 0x28)
LODEntry* std::_STL_alloc_proxy<LODEntry *,LODEntry,std::allocator<LODEntry> >::allocate(unsigned __n)
{
    // @stub
}

// ..\stlport\stl_alloc.h:547
DC_ONLY(0xe9ed0, 0x28)
LODEntry* std::allocator<LODEntry>::allocate(unsigned __n, const void* __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:85
DC_ONLY(0xe9ef8, 0x44)
void std::construct(LODEntry* __p, const LODEntry* __value)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:88
DC_ONLY(0xe9f3c, 0x1C)
LODEntry* std::__uninitialized_copy(LODEntry* __first, LODEntry* __last, LODEntry* __result, LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_algobase.h:382
DC_ONLY(0xe9f58, 0x4E)
LODEntry* std::__copy_backward(LODEntry* __first, LODEntry* __last, LODEntry* __result, std::random_access_iterator_tag __formal, int* __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:255
DC_ONLY(0xe9fa8, 0x1C)
LODEntry* std::__uninitialized_fill_n(LODEntry* __first, unsigned __n, const LODEntry* __x, LODEntry* __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:110
DC_ONLY(0xe9fc4, 0x30)
void std::__destroy_aux(LODEntry* __first, LODEntry* __last, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:70
DC_ONLY(0xe9ff4, 0x3C)
LODEntry* std::__uninitialized_copy_aux(LODEntry* __first, LODEntry* __last, LODEntry* __result, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_uninitialized.h:239
DC_ONLY(0xea030, 0x3C)
LODEntry* std::__uninitialized_fill_n_aux(LODEntry* __first, unsigned __n, const LODEntry* __x, __false_type __formal)
{
    // @stub
}

// ..\stlport\stl_construct.h:59
DC_ONLY(0xea06c, 0x1C)
void std::destroy(LODEntry* __pointer)
{
    // @stub
}

// ..\stlport\stl_construct.h:53
DC_ONLY(0xea088, 0x4)
void std::__destroy_aux()
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\lodfile.cpp:266.  No retail row of its own: /Ob2 folds it
// into the one call site below, where the "LOD" strcpy expands to VC6's
// repne scasb + rep movs pair.
LODHeader::LODHeader()
{
    strcpy(LOD_ID, DATA_COMPGEN(0x0067fa58, lodSignature, "LOD"));
    version = 500;
    numEntries = 0;
    memset(reserved, 0, sizeof(reserved));
}

// E:\gamedcs\lodfile.cpp:240.  Members construct first (header, then the
// subindex vector - whose default constructor copies an uninitialised
// stack allocator byte into +0x17c), and only then does the body clear
// the three raw fields.
VA(0x004fa780, 0x7B)  // order-map constructor, dc 0xe9330
LODFile::LODFile()
{
    fileptr = 0;
    opened = 0;
    dataBuffer = 0;
}

// E:\gamedcs\lodfile.cpp:252.  clear() inlines here in full; the vector
// destructor's deallocate + three-pointer tidy follows it.
VA(0x004fa800, 0x97)  // anchor-global, dc 0xe9374
LODFile::~LODFile()
{
    clear();
}

// E:\gamedcs\lodfile.cpp:430.  The shared failure block sits in the
// MIDDLE of retail's body, not at its tail: all three failing arms jump
// into the -1 pair and the success arm is the function's LAST block.
// One spelling reproduces that - a `fail:` label inside the
// `if (fileptr == 0)` arm that the two early-outs goto.  Writing the
// failure as a duplicated tail return is otherwise instruction-for-
// instruction identical but lets VC6 tail-merge it to the bottom, which
// costs the placement (87.11 and 86.38 measured, both rejected).  The
// seek is issued before the handle is checked, and dataItemIndex really
// is stored twice - once before the check, once in the success arm.
VA(0x004faa70, 0xAB)  // anchor-global, dc 0xe9690
unsigned char LODFile::pointAt(const char* itemName)
{
    if (!opened)
        goto fail;
    Find(0, numEntries, itemName);
    if (matchindex < 0)
        goto fail;
    fseek(fileptr, subindex[matchindex].offset, SEEK_SET);
    dataItemIndex = matchindex;
    if (fileptr == 0) {
fail:
        dataItemIndex = -1;
        dataPos = -1;
        return 0;
    }
    dataItemIndex = matchindex;
    dataPos = 0;
    delete dataBuffer;
    dataBuffer = 0;
    dataBufferSize = 0;
    return 1;
}

// E:\gamedcs\lodfile.cpp:452.  The directory row is COPIED to the stack
// (a 32-byte rep movsd) before any field is read.  Packed entries
// inflate into dataBuffer on first touch and are served from it
// afterwards; unpacked ones stream straight out of the file.  Both
// early-outs share the -1: the second reaches it as `or eax, esi` with
// esi already holding the sentinel it just compared against.
VA(0x004fab20, 0x114)  // anchor-global, dc 0xe96e0
int LODFile::read(void* dest, int numBytes)
{
    if (!opened)
        return -1;
    if (dataItemIndex == -1)
        return -1;

    LODEntry entry = subindex[dataItemIndex];
    if (entry.csize) {
        if (dataBuffer == 0) {
            dataBuffer = new unsigned char[entry.size];
            dataBufferSize = entry.size;
            dataPos = 0;
            unsigned char* packed = new unsigned char[entry.csize];
            fread(packed, 1, entry.csize, fileptr);
            unsigned long destLen = dataBufferSize;
            uncompress(dataBuffer, &destLen, packed, entry.csize);
            delete packed;
        }
        memcpy(dest, dataBuffer + dataPos, numBytes);
        dataPos += numBytes;
    } else {
        fread(dest, 1, numBytes, fileptr);
    }
    return 0;
}
