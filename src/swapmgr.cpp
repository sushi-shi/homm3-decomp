// swapmgr.cpp - E:\gamedcs\swapmgr.cpp (compiland swapmgr.obj)
// HAND-OWNED after admission - INITIALLY GENERATED by `python3 -m homm3.carve carcass`; prototypes are Dreamcast CodeView evidence.
// Retail x86 layout: 24 standalone swapmgr.cpp bodies (23 VA claims + IsLeftHero),
// bracketed by 22 cinit/atexit initializers (excluded class, guard 0x6abaa0), and ~22 DC
// roster functions inlined/folded away in the x86 build (kept DC_ONLY at the tail).
// NOTE: the x86 function order does NOT follow DC source order - claims are in retail RVA order.
#include <va.h>
#include "swapmgr.h"
#include "game.h"
#include "hero.h"
#include "advmgr.h"
#include "bitmap816.h"
#include "remote.h"
#include "widget.h"
#include "winmgr.h"
#include "kb.h"

// swapmgr singleton (bss 0x6a3d30): the ctor stores `this`, Reset/Open/Close consult it.
DATA(0x006a3d30) swapManager* gpSwapManager;

// Selector display mode (bss 0x6a3d08, adjacent to gpSwapManager): Reset clears it,
// DrawSelector dispatches on 0/1/other. All image-wide references sit inside
// swapmgr's bracket, so this compiland owns it (townmgr's gUnnamed6aaa50 precedent).
DATA(0x006a3d08) static int gUnnamed6a3d08;

#if 0  // @carcass -- located, not reconstructed (RVA order)

// E:\gamedcs\swapmgr.cpp:210
VA(0x005aaa80, 0x38E9)  // anchor-callee bitmapBorder/button/CChatEdit/heroWindow/iconWidget ctors + trade2.pcx, dc 0x159328
void TSwapWindow::TSwapWindow(hero** heroes)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:196
VA(0x005ae370, 0x1D)  // anchor-callee ?SendChat@@YIXPBDH@Z + vtable-forward, ret 8, dc 0x15f13c
void CSwapManagerChatEdit::SendChat(const char* sChat, int toWho)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:454
VA(0x005ae390, 0x21)  // anchor-callee scalar-deleting dtor -> ~TSwapWindow (0x5ae3c0), ret 4, dc 0x15f1b0
void* TSwapWindow::`scalar deleting destructor'(unsigned __flags)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:457
VA(0x005ae3c0, 0x6B)  // anchor-callee ??1heroWindow, size 107~=98, dc 0x15c320
void TSwapWindow::~TSwapWindow()
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\swapmgr.cpp:465
// DC symbol is TSwapWindow::UpdateArrows (dc 0x15c384); the carcass mislabelled
// it swapManager::UpdateArrows. `this` reads widget pointers at +0x54/+0x58/+0x5c
// (a swapManager would have selection ints there), and it consults the separate
// gpSwapManager singleton for the side/hero state - so this is the window method.
// Residual: 10/11 blocks byte-exact. B2 differs by one instruction - retail loads
// gpSwapManager via eax then copies to edi and schedules the gpGame load after the
// field_5d test; our CL loads gpSwapManager straight into edi and hoists the gpGame
// load ahead of the test. A register-homing scheduling residual (naming/reordering
// gpSwapManager or gpGame either regressed the CFG or was byte-inert).
VA(0x005ae430, 0xCB)  // corroborates TSwapWindow-ctor sub, send_message widget x7, ret 0, dc 0x15c384
void TSwapWindow::UpdateArrows()
{
    if (!field_54)
        return;
    if (!field_58)
        return;

    swapManager* sm = gpSwapManager;
    if (sm->field_5d)
    {
        if (sm->heroes[0]->owner == gpGame->GetLocalPlayerGamePos())
        {
            field_54->send_message(widget::WIDGET_CLEAR_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
            field_58->send_message(widget::WIDGET_SET_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
        }
        else
        {
            field_54->send_message(widget::WIDGET_SET_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
            field_58->send_message(widget::WIDGET_CLEAR_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
        }
        field_5c->enable(1);
    }
    else
    {
        if (sm->heroes[0]->owner == gpGame->GetLocalPlayerGamePos())
        {
            field_54->send_message(widget::WIDGET_SET_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
            field_58->send_message(widget::WIDGET_CLEAR_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
        }
        else
        {
            field_54->send_message(widget::WIDGET_CLEAR_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
            field_58->send_message(widget::WIDGET_SET_STATUS, widget::WIDGET_ACTIVE | widget::WIDGET_DRAWN);
        }
        field_5c->enable(0);
    }
}

// E:\gamedcs\swapmgr.cpp:583
VA(0x005ae500, 0xA9)  // anchor-callee ??0baseManager + IsHuman, ret 8, dc 0x15c470
swapManager::swapManager(hero* leftHero, hero* rightHero)
{
    heroes[0] = leftHero;
    heroes[1] = rightHero;
    parent = 0;
    border = 0;
    field_48 = -1;
    field_4c = -1;
    field_58 = -1;
    field_50 = -1;
    field_54 = -1;
    field_5d = 1;
    field_5c = 0;
    if (bVideoPaused
        && leftHero->owner != rightHero->owner
        && gpGame->IsHuman(leftHero->owner)
        && gpGame->IsHuman(rightHero->owner))
    {
        field_5c = 1;
        field_5d = (heroes[0]->owner == gpGame->GetLocalPlayerGamePos());
    }
    gpSwapManager = this;
    field_64 = 0;
}

// E:\gamedcs\swapmgr.cpp:617
// Residual (~70%): the four morale/luck clamps to [-3,3]. Message build and the
// five selection resets are byte-exact and the constant 3 CSEs into ebx as retail
// does. The remainder is a register-homing residual: retail keeps the GetMorale/
// GetLuck result live in eax across BOTH clamp comparisons and computes the low
// bound's address lazily inside the taken branch (`jge; lea &lo; jmp`), so the min
// step reads `cmp eax,3` directly; our CL computes `lea &lo` eagerly and reloads
// the max reference (`cmp [eax],3`). Tried and rejected: by-value _cpp_min/_cpp_max
// (CSEs -3 not 3), std::_cpp_min/_cpp_max (this spelling), block-scoped named locals.
VA(0x005ae5b0, 0x19B)  // corroborates 5x -1 selection init at +0x48..+0x58, ret 0, dc 0x15c534
void swapManager::Reset()
{
    field_54 = -1;
    field_50 = -1;
    field_58 = -1;
    field_4c = -1;
    field_48 = -1;
    gUnnamed6a3d08 = 0;

    message msg;
    msg.qualifier = 0;
    msg.mouseX = 0;
    msg.mouseY = 0;
    msg.window = 0;
    msg.id = MESSAGE_WIDGET;
    msg.codeX = 5;
    msg.extra = 0x1000;
    msg.codeY = 0x67;
    parent->BroadcastMessage(&msg);

    msg.codeY = 0x68;
    parent->BroadcastMessage(&msg);

    msg.codeX = 4;
    msg.codeY = 0x6b;
    msg.extra = std::_cpp_min(std::_cpp_max(heroes[0]->GetMorale(0, 0, 1), -3), 3) + 3;
    parent->BroadcastMessage(&msg);

    msg.codeY = 0x6c;
    msg.extra = std::_cpp_min(std::_cpp_max(heroes[1]->GetMorale(0, 0, 1), -3), 3) + 3;
    parent->BroadcastMessage(&msg);

    msg.codeY = 0x6d;
    msg.extra = std::_cpp_min(std::_cpp_max(heroes[0]->GetLuck(0, 0, 1), -3), 3) + 3;
    parent->BroadcastMessage(&msg);

    msg.codeY = 0x6e;
    msg.extra = std::_cpp_min(std::_cpp_max(heroes[1]->GetLuck(0, 0, 1), -3), 3) + 3;
    parent->BroadcastMessage(&msg);
}

#if 0  // @carcass -- located, not reconstructed (RVA order)

// E:\gamedcs\swapmgr.cpp:665
VA(0x005ae750, 0x3A2)  // anchor-callee AddWindow/DisableButtons/CNetMsgHandler + tradesel.pcx, ret 4, dc 0x15c66c
int swapManager::Open(int newPriority)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:518
VA(0x005aeb00, 0x213)  // anchor-callee base CNetMsgHandler::HandleNetMsg + DestroyMsg/HandlePlayerDrop, ret 4, dc 0x15f228
CNetMsg* CSwapMgrNetMsgHandler::HandleNetMsg(CNetMsg* pNetMsg)
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\swapmgr.cpp:789
VA(0x005aed20, 0x9B)  // anchor-callee RemoveWindow/EnableButtons/SetNetMsgHandler, ret 0, dc 0x15ca44
void swapManager::Close()
{
    if (gHeroScreenDraggedArtifact.artifactId != -1)
    {
        heroes[0]->GiveArtifact(&gHeroScreenDraggedArtifact, 0, 0);
        gHeroScreenDraggedArtifact.artifactId = -1;
    }
    border->Dispose();
    gpWindowManager->RemoveWindow(parent);
    delete parent;
    status = 0;
    if (pDPlay)
        pDPlay->SetNetMsgHandler(field_60);
    delete field_64;
    gpAdvManager->status = 1;
    gpAdvManager->EnableButtons();
    gpAdvManager->Reseed(0, 0);
}

// E:\gamedcs\swapmgr.cpp:816
VA(0x005aedc0, 0x140)  // corroborates reads selection +0x48/+0x50 then Bitmap816::Draw/UpdateScreen, ret 0, dc 0x15cadc
void swapManager::DrawSelector()
{
    int x = 0;
    int y = 0;

    if (field_48 == -1)
        return;
    if (field_50 == -1)
        return;

    if (gUnnamed6a3d08)
    {
        int selectedType = heroes[field_48]->army.armies[field_50];
        x = 0x43;
        for (int hero = 0; hero < 2; hero++)
        {
            for (int slot = 0; slot < 7; slot++, x += 0x24)
            {
                if (!(hero == field_48 && slot == field_50))
                {
                    int creature = heroes[hero]->army.armies[slot];
                    if (creature == -1 || creature == selectedType)
                    {
                        border->Draw(0, 0, 0x24, 0x24, gpWindowManager->screenBitmap, x - 2, 0x81, true);
                        gpWindowManager->UpdateScreen(x - 2, 0x81, 0x24, 0x24);
                    }
                }
            }
            x = 0x1e5;
        }
    }
    else
    {
        switch (field_48)
        {
        case kSwapSelectLeft:
            if (field_58 == 0)
            {
                x = field_50 * 36 + 0x41;
                y = 0x81;
            }
            break;
        case kSwapSelectRight:
            if (field_58 == 0)
            {
                x = field_50 * 36 + 0x1e3;
                y = 0x81;
            }
            break;
        }
        border->Draw(0, 0, 0x24, 0x24, gpWindowManager->screenBitmap, x, y, true);
        gpWindowManager->UpdateScreen(x, y, 0x24, 0x24);
    }
}

#if 0  // @carcass -- located, not reconstructed (RVA order)

// E:\gamedcs\swapmgr.cpp:940
VA(0x005aef00, 0x24C)  // corroborates heroes[side].artifact[slot] (8B stride @+0x12d), ret 8, dc 0x15cd2c
void swapManager::UpdateSlot(int iHero, TArtifactSlot slot)
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\swapmgr.cpp:1004
// Residual (93.28%): flow-exact (why-reg flow-distance 0); the 5-slot backpack
// loop and the four arrow-widget broadcasts are byte-exact. The only delta is
// the loop's induction-variable binding - retail keeps the backpack byte offset
// in ebx (stride 8, bound 0x28) with widgetId as a live memory counter, while
// our CL keeps `i` in ebx (stride 1) and folds widgetId to base+i. A
// register-homing residual (the documented dominant class here). Tried and
// rejected: iterating widgetId as the loop var with backpack[widgetId-base]
// (49.49, the subtraction spills), the by-index form above is the closest.
VA(0x005af150, 0x157)  // corroborates hero::get_last_backpack_index, ret 4, dc 0x15cea4
void swapManager::UpdateBackpack(int iHero)
{
    message msg;
    msg.codeX = 0;
    msg.codeY = 0;
    msg.qualifier = 0;
    msg.mouseX = 0;
    msg.mouseY = 0;
    msg.extra = 0;
    msg.window = 0;
    msg.id = MESSAGE_WIDGET;

    int widgetId = 5 * iHero + 0x59;
    for (int i = 0; i < 5; i++)
    {
        message slotMsg;
        slotMsg.id = 0;
        slotMsg.codeX = 0;
        slotMsg.codeY = 0;
        slotMsg.qualifier = 0;
        slotMsg.mouseX = 0;
        slotMsg.mouseY = 0;
        slotMsg.extra = 0;
        slotMsg.window = 0;

        type_artifact art = heroes[iHero]->backpack[i];
        slotMsg.id = MESSAGE_WIDGET;
        slotMsg.codeY = widgetId;
        if (art.artifactId == -1)
        {
            slotMsg.codeX = 6;
        }
        else
        {
            slotMsg.extra = art.artifactId;
            slotMsg.codeX = 4;
            parent->BroadcastMessage(&slotMsg);
            slotMsg.codeX = 5;
        }
        slotMsg.extra = 4;
        parent->BroadcastMessage(&slotMsg);
        widgetId++;
    }

    msg.codeX = (heroes[iHero]->get_last_backpack_index() + 1 > 5) ? 6 : 5;
    msg.extra = 0x1000;
    msg.codeY = iHero + 0x63;
    parent->BroadcastMessage(&msg);

    msg.codeY = iHero + 0x65;
    parent->BroadcastMessage(&msg);

    msg.codeX = (heroes[iHero]->get_last_backpack_index() + 1 <= 5) ? 6 : 5;
    msg.extra = 2;
    msg.codeY = iHero + 0x63;
    parent->BroadcastMessage(&msg);

    msg.codeY = iHero + 0x65;
    parent->BroadcastMessage(&msg);
}

#if 0  // @carcass -- located, not reconstructed (RVA order)

// E:\gamedcs\swapmgr.cpp:1031
VA(0x005af2b0, 0x2DD)  // anchor-callee SplitArmy/ViewArmy + SwapMons(0x5b0da0), ret 0x10, dc 0x15cf54
void swapManager::HandleMonster(int iHero, int iMonster, int bRightMouse, unsigned char bShift)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:1106
VA(0x005af590, 0x3F7)  // anchor-callee hero::equip_artifact + HeroFn artifact family, ret 0xc, dc 0x15d150
void swapManager::handle_artifact_click(long side, long id, unsigned char right_click)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:1168
VA(0x005af990, 0x251)  // anchor-callee hero::add_to_backpack/remove_backpack_artifact/get_backpack_error, ret 0xc, dc 0x15d2e0
void swapManager::handle_backpack_click(long side, long id, unsigned char right_click)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:1208
VA(0x005afbf0, 0x17A)  // corroborates 2x hero copy + TransmitRemoteData (CHeroUpdateMsg build+send), ret 0, dc 0x15d440
void swapManager::SendHeroUpdate()
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp - DC name unresolved (not a distinct DC roster symbol).
// TENTATIVE: swapManager teardown - frees two std::string members near +0x3f2/+0x884,
// reached via EH unwind funclet 0x22b281. Retail body exists; claimed to hold the RVA slot.
VA(0x005afd70, 0x7B)  // corroborates 2x basic_string _Tidy + EH-unwind caller, ret 0, retail-only
void swapManager::~swapManager()
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:1222
VA(0x005afdf0, 0xABB)  // anchor-callee ConvertToHover + message dispatcher (HandleMonster/handle_*_click/SetRolloverText/OnWidgetDeselect), ret 4, dc 0x15d4ac
int swapManager::Main(message* msg)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:1793
VA(0x005b08b0, 0x4EF)  // anchor-callee type_artifact::get_rollover_text + GetLuck/GetMorale/GetNthSS, ret 4, dc 0x15e308
void swapManager::SetRolloverText(int codeY)
{
    // @stub
}

// E:\gamedcs\swapmgr.cpp:2030
VA(0x005b0da0, 0x141)  // anchor-callee armyGroup::Swap + GetNumArmies, size 321~=322, ret 0, dc 0x15e8bc
void swapManager::SwapMons()
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\swapmgr.cpp:962
VA(0x005b0ef0, 0x1D4)  // corroborates loops artifact slots calling UpdateSlot(0x5aef00) + sprintf hero names, ret 0, dc 0x15cdbc
void swapManager::update_all_slots()
{
    message msg;
    msg.id = MESSAGE_WIDGET;
    msg.qualifier = 0;
    msg.mouseX = 0;
    msg.mouseY = 0;
    msg.window = 0;

    int i;
    for (int side = 0; side < 2; side++)
    {
        msg.codeX = 3;
        msg.extraText = gText;
        for (i = 0; i < 4; i++)
        {
            msg.codeY = 3 + side * 5 + i;
            signed char stat = heroes[side]->stats[i];
            int val;
            if (stat > 99)
                val = 99;
            else if (stat > 0)
                val = stat;
            else
                val = (i >= 2);
            sprintf(gText, DATA_COMPGEN(0x00660a1c, decimalFormat, "%d"), val);
            parent->BroadcastMessage(&msg);
        }
        for (i = 0; i < 7; i++)
        {
            msg.codeY = 0xd + side * 7 + i;
            if (heroes[side]->army.armies[i] == -1)
            {
                msg.codeX = 6;
                msg.extra = 4;
            }
            else
            {
                msg.codeX = 5;
                msg.extra = 4;
                parent->BroadcastMessage(&msg);
                msg.codeX = 4;
                msg.extra = heroes[side]->army.armies[i] + 2;
            }
            parent->BroadcastMessage(&msg);
        }
        for (i = 0; i < 7; i++)
        {
            msg.codeY = 0x41 + side * 7 + i;
            if (heroes[side]->army.armies[i] == -1)
            {
                msg.codeX = 6;
                msg.extra = 4;
            }
            else
            {
                msg.codeX = 5;
                msg.extra = 4;
                parent->BroadcastMessage(&msg);
                msg.codeX = 3;
                sprintf(gText, DATA_COMPGEN(0x00660a1c, decimalFormat, "%d"),
                        heroes[side]->army.numTroops[i]);
                msg.extraText = gText;
            }
            parent->BroadcastMessage(&msg);
        }
    }

    for (int iHero = 0; iHero < 2; iHero++)
        for (int slot = 0; slot < 0x13; slot++)
            UpdateSlot(iHero, slot);
}

#if 0  // @carcass -- located, not reconstructed (RVA order)

// E:\gamedcs\swapmgr.cpp:2165
VA(0x005b10d0, 0x2A8)  // anchor-callee hero::rotate_backpack_left/right + UpdateBackpack/SendHeroUpdate, ret 8, dc 0x15ec58
void swapManager::OnWidgetDeselect(message* msg, int* exitFlag)
{
    // @stub
}

#endif  // @carcass

// E:\gamedcs\swapmgr.cpp:2231
VA(0x005b1380, 0x1C)
bool swapManager::IsLeftHero()
{
    if (heroes[0]->owner == gpGame->GetLocalPlayerGamePos())
        return true;
    return false;
}

#if 0  // @carcass -- DC roster functions with NO standalone retail body:
       // inlined (/Ob2 single-call-site or trivial) or ICF-folded. Order-exempt (DC_ONLY).

// E:\gamedcs\swapmgr.cpp:655
DC_ONLY(0x15c648, 0x24)
int swapManager::DrawSwapWin()
{
    // @stub -- inlined (36 B accessor)
}

// E:\gamedcs\swapmgr.cpp:914
DC_ONLY(0x15cccc, 0x5E)
void swapManager::update_artifact_widget(long id, TArtifact artifact)
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:977
DC_ONLY(0x15ce20, 0x84)
void swapManager::UpdateBackpackItem(int iHero, int i)
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:1660
DC_ONLY(0x15de34, 0xC)
int swapManager::ExitSwapManager(message* msg)
{
    // @stub -- inlined (12 B forwarder)
}

// E:\gamedcs\swapmgr.cpp:1667
DC_ONLY(0x15de40, 0x4C8)
void swapManager::swap_side()
{
    // @stub -- inlined into Main (single call site)
}

// E:\gamedcs\swapmgr.cpp:2024
DC_ONLY(0x15e85c, 0x5E)
void swapManager::ViewMon()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2072
DC_ONLY(0x15ea00, 0x190)
void swapManager::Update()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2140
DC_ONLY(0x15eb90, 0x1A)
void swapManager::OnChatUpdate()
{
    // @stub -- inlined (26 B)
}

// E:\gamedcs\swapmgr.cpp:2147
DC_ONLY(0x15ebac, 0xAA)
void swapManager::HandleHeroUpdateMsg(CNetMsg* pNetMsg)
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2241
DC_ONLY(0x15ee24, 0x2A)
unsigned char swapManager::IsRightHero()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2251
DC_ONLY(0x15ee50, 0x22)
hero* swapManager::GetOtherHero()
{
    // @stub -- inlined (accessor)
}

// E:\gamedcs\swapmgr.cpp:2259
DC_ONLY(0x15ee74, 0x22)
hero* swapManager::GetOurHero()
{
    // @stub -- inlined (accessor)
}

// E:\gamedcs\swapmgr.cpp:2267
DC_ONLY(0x15ee98, 0x82)
unsigned char swapManager::CanModHero(int hero)
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2287
DC_ONLY(0x15ef1c, 0x44)
void swapManager::OnReceiveFromAlly()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:2298
DC_ONLY(0x15ef60, 0x70)
void swapManager::OnGiveMeStuffMsg()
{
    // @stub -- inlined
}

// E:\gamedcs\basemgr.h:41
DC_ONLY(0x15efd0, 0x4)
void baseManager::SetStatus(short newStatus)
{
    // @stub -- inlined (4 B, header inline)
}

// E:\gamedcs\swapmgr.cpp:120
DC_ONLY(0x15efd4, 0x90)
void CHeroUpdateMsg::CHeroUpdateMsg(hero* left, hero* right)
{
    // @stub -- inlined into SendHeroUpdate
}

// E:\gamedcs\swapmgr.cpp:130
DC_ONLY(0x15f064, 0x20)
void CTradeRequestDoneMsg::CTradeRequestDoneMsg()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:151
DC_ONLY(0x15f084, 0x20)
void CGiveMeStuffMsg::CGiveMeStuffMsg()
{
    // @stub -- inlined
}

// E:\gamedcs\swapmgr.cpp:192
DC_ONLY(0x15f0a4, 0x98)
void CSwapManagerChatEdit::CSwapManagerChatEdit(int textWidgetX, int textWidgetY, int textWidgetWidth, int textWidgetHeight, int textStringSize, char* textString, char* textFontName, int colorIndex, font::EJustify justification, char* backgroundIconName, int backgroundFrame, int textWidgetId, int textWidgetStyle, int iReadType, int textInsetX, int textInsetY)
{
    // @stub -- inlined into TSwapWindow ctor (single call site)
}

// E:\gamedcs\swapmgr.cpp:201
DC_ONLY(0x15f164, 0x34)
void* CSwapManagerChatEdit::`scalar deleting destructor'(unsigned __flags)
{
    // @stub -- folded/inlined
}

// E:\gamedcs\swapmgr.cpp:201
DC_ONLY(0x15f198, 0x18)
void CSwapManagerChatEdit::~CSwapManagerChatEdit()
{
    // @stub -- inlined (24 B)
}

// E:\gamedcs\swapmgr.cpp:512
DC_ONLY(0x15f1e4, 0x44)
void CSwapMgrNetMsgHandler::CSwapMgrNetMsgHandler()
{
    // @stub -- inlined (68 B)
}

#endif  // @carcass
