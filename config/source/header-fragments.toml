# Canonical bodies shared with the Mac declaration views. These are source
# file/include-position mappings, not function identities or gate exemptions.

[[fragments]]
fragment = "include/inline/mapcell_monster_data_ctor.inl"
owner = "include/mapcell.h"
evidence = "Existing in-class MonsterData constructor body extracted unchanged at its original definition position; DC MapCell.h:735 and Mac readMonsterData construct the local record before its string read."

[[fragments]]
fragment = "include/ai_player_set_attack_bonuses.inl"
owner = "include/ai_player.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/ai_player_get_resource_value.inl"
owner = "include/ai_player.h"
evidence = "Existing in-class getResourceValue definition extracted unchanged; ai_player.h includes it at its original definition position."

[[fragments]]
fragment = "include/game_get_hero.inl"
owner = "include/game.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/game_get_team.inl"
owner = "include/game.h"
evidence = "Existing in-class getTeam body and VA claim extracted unchanged; game.h includes it at its original definition position and the Mac declaration view shares the same body."

[[fragments]]
fragment = "include/game_town_already_built.inl"
owner = "include/game.h"
evidence = "Existing ordinary inline query and evidence comment extracted unchanged; game.h includes it at the original definition position and the Mac declaration view shares the same body."

[[fragments]]
fragment = "include/message_record.h"
owner = "include/struct.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/mousemgr_is_vis.inl"
owner = "include/mousemgr.h"
evidence = "Existing in-class mouseManager::isVis definition extracted unchanged at its original position; PHILAI Mac view shares that body and the Mac MoveHero caller tests m_hideCount at +0x68."

[[fragments]]
fragment = "include/type_artifact_constructors.inl"
owner = "include/hero.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/army_is.inl"
owner = "include/army.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/army_offset_to_front.inl"
owner = "include/army.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/army_get_ai_target.inl"
owner = "include/army.h"
evidence = "Existing canonical getAITarget inline body and Dreamcast line prefix extracted unchanged at the original Army.h definition position."

[[fragments]]
fragment = "include/inline/army_get_ai_target_time.inl"
owner = "include/army.h"
evidence = "Existing canonical zero-argument getAITargetTime inline body and Dreamcast line prefix extracted unchanged at the original Army.h definition position."

[[fragments]]
fragment = "include/inline/army_is_incapacitated.inl"
owner = "include/army.h"
evidence = "Existing canonical isIncapacitated inline body and VA/DC evidence prefix extracted unchanged at the original Army.h definition position."

[[fragments]]
fragment = "include/inline/army_cannot_attack.inl"
owner = "include/army.h"
evidence = "Existing canonical cannotAttack inline body and evidence prefix extracted unchanged at the original Army.h definition position."

[[fragments]]
fragment = "include/inline/army_get_spell_time.inl"
owner = "include/army.h"
evidence = "Existing canonical inline body and attached Dreamcast source prefix extracted unchanged at the original definition position; Dreamcast find_AI_targets names its source call and both retail ports inline the field read."

[[fragments]]
fragment = "include/inline/army_clear_ai_values.inl"
owner = "include/army.h"
evidence = "Dreamcast ai.cpp:2622 names the canonical inline helper. Complete x86 findAITargets 0x422b20 and Mac 0:0x25100 retain four clears in target/value/possibleTargets/time order with no expectedDamage write; the older DC helper's field set is documented beside this single body."

[[fragments]]
fragment = "include/inline/combat_get_current_army.inl"
owner = "include/cmbtmgr.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/creaturetype_get_army_name.inl"
owner = "include/creaturetype.h"
evidence = "Existing canonical GetArmyName inline body with VA and Dreamcast source prefix extracted unchanged; creaturetype.h includes it at its original definition position, and the Mac KB view shares that body."

[[fragments]]
fragment = "include/inline/textresource_get_text.inl"
owner = "include/textresource.h"
evidence = "Existing canonical getText inline body with its VA and Dreamcast source prefix extracted unchanged at the original TTextResource class position; the Mac KB view shares this body."

[[fragments]]
fragment = "include/inline/textresource_index.inl"
owner = "include/textresource.h"
evidence = "Existing canonical TTextResource::operator[] inline body with its Dreamcast source prefix extracted unchanged at the original class position; the Mac KB view shares this body."

[[fragments]]
fragment = "include/inline/combat_valid_hex.inl"
owner = "include/cmbtmgr.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/search_get_hex.inl"
owner = "include/findpath.h"
evidence = "Existing canonical body extracted without changing expanded C++ tokens; the owner includes it at its original definition position."

[[fragments]]
fragment = "include/inline/hero_get_primary_skill.inl"
owner = "include/hero.h"
evidence = "Existing in-class getPrimarySkill definition and VA claim extracted unchanged; hero.h includes it at its original definition position."

[[fragments]]
fragment = "include/inline/hero_get_value_of_knowledge.inl"
owner = "include/hero.h"
evidence = "Existing in-class getValueOfKnowledge definition extracted unchanged; hero.h includes it at its original definition position."

[[fragments]]
fragment = "include/inline/hero_get_value_of_power.inl"
owner = "include/hero.h"
evidence = "Existing in-class getValueOfPower definition extracted unchanged; hero.h includes it at its original definition position."

[[fragments]]
fragment = "include/inline/hero_get_artifact.inl"
owner = "include/hero.h"
evidence = "Existing in-class getArtifact definition extracted unchanged; hero.h includes it at its original definition position and the Mac HERO view shares the same body."

[[fragments]]
fragment = "include/inline/hero_adjust_primary_skill.inl"
owner = "include/hero.h"
evidence = "Existing in-class adjustPrimarySkill definition extracted unchanged; hero.h includes it at its original definition position and the Mac HERO view shares the same body. DC hero::remove_artifact line 4926 names its call."

[[fragments]]
fragment = "include/inline/ai_parameters_get_group.inl"
owner = "include/ai_tactical.h"
evidence = "Existing canonical getGroup body extracted without changing expanded C++ tokens; ai_tactical.h includes it at its original definition position, and Dreamcast ai.cpp:1636/1638 records two calls."

[[fragments]]
fragment = "include/game_is_human_team.inl"
owner = "include/game.h"
evidence = "Canonical guarded IsHumanTeam scan restored at its existing class helper site, replacing the duplicate unguarded loop. Windows 0x42b9e0, Mac 0:0x2d3e4 and DC 0x37f64 prove this identity; the VA previously mislabeled the player-number wrapper."

[[fragments]]
fragment = "include/game_is_human_ally.inl"
owner = "include/game.h"
evidence = "DC Game.h:1370-1371 is_human_ally calls GetTeam then IsHumanTeam. Its one canonical wrapper body is shared with the Mac view; the team scan owns the separate retained VA."

[[fragments]]
fragment = "include/inline/game_get_town.inl"
owner = "include/game.h"
evidence = "Existing canonical game::getTown(int) body and attached VA/DC evidence prefix extracted unchanged at the original class position; Mac AI aiCheckRetreat inlines its signed -1 sentinel and town-table lookup."

[[fragments]]
fragment = "include/inline/game_on_same_team.inl"
owner = "include/game.h"
evidence = "Existing canonical game::onSameTeam body and attached VA/DC evidence prefix extracted unchanged at its original class position; the Mac KB loss dispatcher expands its guarded team-byte comparison in the town and hero arms."

[[fragments]]
fragment = "include/inline/netmsg_cnetmsg_ctor.inl"
owner = "include/netmsg.h"
evidence = "Existing canonical CNetMsg constructor body and attached VA claim extracted unchanged at its original class position; Mac KB sendPlayerLost expands its five base fields."

[[fragments]]
fragment = "include/inline/netmsg_player_lost_ctor.inl"
owner = "include/netmsg.h"
evidence = "Existing canonical CPlayerLostMsg constructor body and source evidence extracted unchanged at its original class position; Mac KB sendPlayerLost builds this same loss payload."

[[fragments]]
fragment = "include/inline/netmsg_player_won_ctor.inl"
owner = "include/netmsg.h"
evidence = "Existing canonical CPlayerWonMsg constructor and Dreamcast prefix extracted unchanged at its original class position; the Mac sender and Windows DisplayVCWinLoss sites share this source body."

[[fragments]]
fragment = "include/inline/loss_condition_ctor.inl"
owner = "include/victorylossconditions.h"
evidence = "Existing canonical LossConditionStruct default constructor and retained VA claim extracted unchanged at its original class position; Mac KB sendPlayerLost initializes those fields before assigning the condition."

[[fragments]]
fragment = "include/inline/game_get_curr_hero.inl"
owner = "include/game.h"
evidence = "Existing canonical game::getCurrHero body extracted unchanged at its original class position; Mac PHILAI moveHero expands its acting-player hero lookup."

[[fragments]]
fragment = "include/inline/game_get_num_map_levels.inl"
owner = "include/game.h"
evidence = "Existing canonical game::getNumMapLevels body extracted unchanged at its original class position; Mac PHILAI moveHero expands the map layer count in its danger-zone byte count."

[[fragments]]
fragment = "include/inline/mapcell_get_num_levels.inl"
owner = "include/mapcell.h"
evidence = "Existing canonical NewfullMap::getNumLevels body extracted unchanged at its original out-of-class inline position; Mac PHILAI moveHero reads the map two-level flag and adds one."

[[fragments]]
fragment = "include/inline/search_set_danger_zones.inl"
owner = "include/findpath.h"
evidence = "Existing canonical searchArray::setDangerZones body extracted unchanged at its original class position; Mac PHILAI moveHero stores and clears the danger-zone pointer at searchArray+0x60."

[[fragments]]
fragment = "include/inline/hero_get_backpack.inl"
owner = "include/hero.h"
evidence = "Existing canonical hero::getBackpack(long) inline body and attached DC source prefix extracted unchanged at the original class position; Mac AI aiCheckRetreat directly reads packed backpack artifact bytes."

[[fragments]]
fragment = "include/inline/game_get_current_turn.inl"
owner = "include/game.h"
evidence = "Existing game::getCurrentTurn definition extracted unchanged at Game.h:1390 in the original header position; the Mac KB view shares its canonical inline body."

[[fragments]]
fragment = "include/inline/town_has_building.inl"
owner = "include/town.h"
evidence = "Existing canonical town::hasBuilding definition and VA claim extracted unchanged at its original class position; Mac aiEnterTown reads the town built/active masks for MAGE_GUILD and EXTRA_0 checks."

[[fragments]]
fragment = "include/inline/town_get_building_mask.inl"
owner = "include/town.h"
evidence = "Existing canonical town::getBuildingMask in-class body and attached Dreamcast Town.h:299/300 source prefix extracted unchanged at its original position; Mac canBuild reads the active mask at town+0x14c."

[[fragments]]
fragment = "include/inline/town_is_castle.inl"
owner = "include/town.h"
evidence = "Existing canonical town::isCastle in-class body and attached Dreamcast Town.h:337 source prefix extracted unchanged at its original position; Mac buildBuilding tests fort, citadel and castle mask bits."

[[fragments]]
fragment = "include/inline/town_is_capitol.inl"
owner = "include/town.h"
evidence = "Existing canonical town::isCapitol in-class body and attached Dreamcast Town.h:342 source prefix extracted unchanged at its original position; Mac buildBuilding tests the capitol mask bit."

[[fragments]]
fragment = "include/inline/army_is_active.inl"
owner = "include/army.h"
evidence = "Existing canonical army::isActive body and attached Dreamcast Army.h:830 source prefix extracted unchanged at the original inline definition position; Mac AI aiCheckRetreat inlines its active troop test."

[[fragments]]
fragment = "include/inline/cpp_max.inl"
owner = "include/DC_precompiledheaders.h"
evidence = "Existing canonical Dreamcast cppMax template with its DC_precompiledheaders.h:33 source prefix extracted unchanged at its original definition position; the Mac AI view uses this selector beneath max(int,int)."

[[fragments]]
fragment = "include/inline/max_int.inl"
owner = "include/includes.h"
evidence = "Existing canonical max(int,int) wrapper with its includes.h:97 and dc 0x1ef28 source prefix extracted unchanged at its original definition position; Mac AI aiCheckRetreat uses the same copy-owning wrapper."

[[fragments]]
fragment = "include/inline/hero_get_max_mana.inl"
owner = "include/hero.h"
evidence = "Existing canonical hero::getMaxMana in-class body extracted unchanged at the original definition position; Dreamcast Hero.h:634 identifies the helper, and Mac initialize expands it before calling getIntelligenceFactor."

[[fragments]]
fragment = "include/inline/ai_attack_chooser_get_attack_time.inl"
owner = "include/ai_tactical.h"
evidence = "Existing canonical getAttackTime() in-class body and attached Dreamcast ai_tactical.h:471..482 source prefix extracted unchanged at its original position; Mac AI chooseMeleeTarget reads the selected attack time."

[[fragments]]
fragment = "include/inline/ai_attack_chooser_get_best_hex.inl"
owner = "include/ai_tactical.h"
evidence = "Existing canonical getBestHex() in-class body extracted unchanged at its original position; Mac AI chooseMeleeTarget reads the selected hex."

[[fragments]]
fragment = "include/inline/ai_attack_chooser_get_hex_value.inl"
owner = "include/ai_tactical.h"
evidence = "Existing canonical getHexValue() in-class body extracted unchanged at its original position; Mac AI chooseMeleeTarget reads the selected value."

[[fragments]]
fragment = "include/inline/combat_grid_y.inl"
owner = "include/cmbtmgr.h"
evidence = "Existing canonical combatManager::gridY static in-class body and Dreamcast CmbtMgr.h:1513 prefix extracted unchanged at its original position; Mac AI chooseMeleeTarget indexes the castle wall columns by combat row."

[[fragments]]
fragment = "include/inline/obscuring_object_get_location.inl"
owner = "include/hero.h"
evidence = "Existing canonical type_obscuring_object::getLocation body and VA claim extracted unchanged at its original class position; Mac attemptTeleport builds the same packed map point."

[[fragments]]
fragment = "include/inline/hero_get_mana_cost.inl"
owner = "include/hero.h"
evidence = "Existing canonical one-argument hero::getManaCost wrapper and Dreamcast source prefix extracted unchanged at its original class position; Mac attemptTeleport calls the terrain-aware overload."

[[fragments]]
fragment = "include/inline/hero_get_spell_level.inl"
owner = "include/hero.h"
evidence = "Existing canonical one-argument hero::getSpellLevel wrapper and Dreamcast source prefix extracted unchanged at its original class position; Mac attemptTeleport calls the terrain-aware overload."

[[fragments]]
fragment = "include/inline/hero_get_target.inl"
owner = "include/hero.h"
evidence = "Existing canonical hero::getTarget body and Dreamcast source prefix extracted unchanged at its original class position; Mac attemptTeleport reads the packed path target."

[[fragments]]
fragment = "include/inline/hero_spell_is_available.inl"
owner = "include/hero.h"
evidence = "Existing canonical hero::spellIsAvailable body and Dreamcast source prefix extracted unchanged at its original class position; Mac attemptTeleport reads the spell availability byte."

[[fragments]]
fragment = "include/inline/type_point_ctor.inl"
owner = "include/struct.h"
evidence = "Existing canonical packed type_point(short,short,short) constructor and VA claim extracted unchanged at its original struct position; Mac attemptTeleport builds packed map points through this constructor."

[[fragments]]
fragment = "include/inline/game_get_cell.inl"
owner = "include/game.h"
evidence = "Existing canonical game::getCell(type_point) inline body and VA/DC prefix extracted unchanged at its original out-of-class definition position; Mac attemptTeleport expands its map-cell address calculation."

[[fragments]]
fragment = "include/inline/mapcell_z_cell.inl"
owner = "include/mapcell.h"
evidence = "Existing canonical NewfullMap::zCell(int,int,int) inline body and VA/DC prefix extracted unchanged at its original definition position; Mac attemptTeleport computes the same 0x22-byte cell stride."

[[fragments]]
fragment = "include/inline/mapcell_cell_xyz.inl"
owner = "include/mapcell.h"
evidence = "Existing canonical NewfullMap::cell(int,int,int) forwarding body and DC source prefix extracted unchanged at its original definition position; Mac attemptTeleport expands this call through zCell."

[[fragments]]
fragment = "include/inline/search_get_cell.inl"
owner = "include/findpath.h"
evidence = "Existing canonical searchArray::getCell(type_point,bool) inline body and VA/DC source prefix extracted unchanged at its original class position; Mac valueOfHiring expands its indexed search cell lookup."

[[fragments]]
fragment = "include/inline/hero_set_primary_skill.inl"
owner = "include/hero.h"
evidence = "Existing canonical DC-attested hero::setPrimarySkill(int,int) inline body extracted unchanged at its original class position; Mac initialize(short) expands the same byte store from the full-width parameter."

[[fragments]]
fragment = "include/inline/combat_grid_x.inl"
owner = "include/cmbtmgr.h"
evidence = "Existing canonical combatManager::gridX static in-class body extracted unchanged at its original position; Mac faerie dragon chooser uses its modulo-17 column test."

[[fragments]]
fragment = "include/inline/combat_in_invisible_column.inl"
owner = "include/cmbtmgr.h"
evidence = "Existing canonical combatManager::inInvisibleColumn static in-class body and DC source prefix extracted unchanged at its original position; Mac faerie dragon chooser expands its valid-hex and invisible-edge checks."

[[fragments]]
fragment = "include/inline/game_artifact_wizards_well_id.inl"
owner = "src/game.cpp"
evidence = "Existing source-owned const int Wizard's Well artifact ID at the original game.cpp definition position, used by Windows and Mac game::perDay; Mac target passes literal 0x8a after the canonical constant is visible."

[[fragments]]
fragment = "include/inline/widget_hide.inl"
owner = "include/widget.h"
evidence = "Existing canonical widget::hide inline body and Dreamcast header-source prefix extracted unchanged at its original class position; the Mac campaign-brief flag loop expands its status-message call."

[[fragments]]
fragment = "include/inline/widget_show.inl"
owner = "include/widget.h"
evidence = "Existing canonical widget::show inline body extracted unchanged at its original class position; the Mac campaign-brief flag loop expands its status-message call."

[[fragments]]
fragment = "include/inline/t_limit.inl"
owner = "include/includes.h"
evidence = "Existing canonical Dreamcast-typed tLimit template and source prefix extracted unchanged at its original includes.h position; Mac screenScroll expands the same value clamp."

[[fragments]]
fragment = "include/inline/limit.inl"
owner = "include/includes.h"
evidence = "Existing canonical Dreamcast-typed int limit wrapper and source prefix extracted unchanged at its original includes.h position; Mac screenScroll expands it before indexing scroll speeds."

[[fragments]]
fragment = "include/inline/advmgr_scroll_speed_inc.inl"
owner = "src/advmgr.cpp"
evidence = "Existing source-owned static scroll-speed constant and DATA claim extracted unchanged at its original advmgr.cpp position. Windows and Mac screenScroll index its full three-int {1,2,3} table."
