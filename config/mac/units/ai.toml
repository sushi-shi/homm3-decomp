mode = "paired_bodies"
flags = ["-O3", "-proc", "750", "-nolink"]
helpers = [0x420060]
source_helpers = ["findAttackHexes(const army* ourArmy, const army* enemy, const searchArray* currentSearchArray, std::vector<long>& result)"]
